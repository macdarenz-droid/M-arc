# Execution ledger

Prepared 2026-09-21. **This is a new workflow: app implementation has not started.** Documentation review is not feature verification.

Target executor: Claude Sonnet, Medium effort. Repository: `macdarenz-droid/M-arc`. Working branch: `claude/phase-9-readiness-preference-ckw91g`.

## Baseline evidence

Application baseline: `564df825a281eb1c95cf71ab8bc82355b4f3c900`. Previous full gate: [run 35602531054](https://github.com/macdarenz-droid/M-arc/actions/runs/35602531054), both jobs successful. Historical local results: 627 app tests; 87 passing proxy tests, eight explicitly conditional skips; both typechecks, build and five-theme gate passed. P00 must verify the actual execution head again.

This planning package was checked against current source by separate brain/data and integration/regression reviewers. It does not establish that the proposed new modules, tests or features exist.

Planning review corrections incorporated: normal intent has no inferred effort ceiling; cap-breach precedence retains valid partial evidence; invalidated entries do not require live target projections; ordinary input corrections remain assessable; deleted work retains row provenance; missing work is distinct from missing effort; journal replay validates entry lifecycle and fresh Undo IDs; overdue review dates survive reload; patch tests respect dependencies; informational cue dismissals use the new store; chat reset invalidates every late handler and clears transient state. Document checks cover seven files, relative links, UTF-8, code fences, eleven ordered stages and 95 unique regression IDs.

## Status

| Stage | Deliverable | Status | Commit / local evidence / exact CI |
|---|---|---|---|
| P00 | Verify current baseline | VERIFIED | 7d15d4c9dabda5f4add72ad37bf52d3582e19f6a; [run 35617178195](https://github.com/macdarenz-droid/M-arc/actions/runs/35617178195), source-gate + android-gate both success |
| P01 | Moment selector, tone, shared dismissals | VERIFIED | 62d097c1a3ecb0b02036df74e854299670b9b34b; [run 35619601814](https://github.com/macdarenz-droid/M-arc/actions/runs/35619601814), source-gate + android-gate both success |
| P02 | Presence and local explanation surfaces | VERIFIED — all six named surfaces addressed: Today/Train/History/Body integrated, Coach and Settings verified exempt (see below) | ab9f89d9a03d58c2705053e9ca9cddc1366825c0; [run 35630547553](https://github.com/macdarenz-droid/M-arc/actions/runs/35630547553), source-gate + android-gate both success |
| P03 | Shared guarded contextual Ask | VERIFIED — every "Do" item shipped: remote-enabled guard, request-generation invalidation, shared host mount, verified no-nested-modal, contextual prefill, and a dedicated typed-draft-survives-navigation scenario (see below) | 6bf05ee2c1e0c74feb596894c60b5fe48f9dc9f7 (P03.6, final); [run 35644471679](https://github.com/macdarenz-droid/M-arc/actions/runs/35644471679), source-gate + android-gate both success |
| P04 | Optional intent and normalization | VERIFIED — data model, capture, validation/normalization (incl. lifecycle replay), active-session UI and NEW tests/session-intent.test.ts (28 tests) all shipped | 6c066edcd07bf056351c987952e215aa207040b1; [run 35646654197](https://github.com/macdarenz-droid/M-arc/actions/runs/35646654197), source-gate + android-gate both success |
| P05 | Prospective agreements and evidence integrity | IN PROGRESS — P05.1 reviewed/verified; P05.2 exact-head gates pass; P05.3 closes an origin/link replay-validation gap locally, but exact-head CI and the independent mutation review remain outstanding | pending P05.3 commit; 797 app tests, 87 proxy tests / 8 skipped, build + five-theme browser gate green |
| P06 | Fair plan-fit projection | VERIFIED — pure projector, F01–F16/I07/J06/J07 outcome tests and exact-head gate all pass | 18c86f7048fdf31894bce74d487197149fa7d8f8; [run 35681530569](https://github.com/macdarenz-droid/M-arc/actions/runs/35681530569), source-gate + android-gate both success |
| P07 | Purpose-aware debrief and copy | VERIFIED — finish/History evidence, Today/Coach summary, tone equality and five-theme layout gate pass | b77bad419404c5f64f480bfd2a1eade209678c3a; [run 35683476449](https://github.com/macdarenz-droid/M-arc/actions/runs/35683476449), source-gate + android-gate both success |
| P08 | Optional personal objective | IN PROGRESS — implementation and exact-head gates pass; independent persistence review remains outstanding | 292330651758bcf94aefa5b0a4026b804b1bbd82; [run 35686321589](https://github.com/macdarenz-droid/M-arc/actions/runs/35686321589), source-gate + android-gate both success |
| P09 | Evidence review and surface integration | VERIFIED — pure review, Coach/Today/Body/Train integration and exact-head gates pass | a2a1f48217a61fbf9245c509ea7ec0f7b2b6ff21; [run 35688951369](https://github.com/macdarenz-droid/M-arc/actions/runs/35688951369), source-gate + android-gate both success |
| P10 | Integrated audit, exact-head gate and APK | IN PROGRESS — combined audit and exact-head source/Android gates pass; required independent review remains unavailable | 621c32a6b42547e6adf0d00687b5e020457ff79d; [run 35690338125](https://github.com/macdarenz-droid/M-arc/actions/runs/35690338125), source-gate + android-gate success; APK artifact 10678304111 |

Allowed execution states: NOT STARTED, IN PROGRESS, LOCAL VERIFIED / CI PENDING, VERIFIED, BLOCKED. VERIFIED needs observed required evidence, not an expectation. Record blockers precisely; do not use BLOCKED for an ordinary failing test that can be fixed.

## Append one record per patch or corrective subpatch

```text
Patch / date / executor:
Starting local and remote SHA:
User-visible change:
Owned files and any reconciled baseline drift:
Architecture decisions (or none):
Regression IDs → concrete test/scenario names:
Focused failure reproduction and result:
Full commands → exit/result/test counts/skips:
Browser evidence paths, themes/widths and console/network result:
Reviewer identity, findings, fixes and re-review:
Commit and verified remote SHA:
Exact-head workflow URL + source/Android conclusions:
Known limits / remaining proof:
Next concrete action:
```

Record a commit's CI result in the next patch or final report, avoiding a self-referential final-SHA commit loop. Final report must account for all regression IDs and the final artifact, even if this ledger was committed before that run completed.

```text
Patch / date / executor: P00 / 2026-09-21 / Claude Sonnet 5
Starting local and remote SHA: local was c3f4675 (stale), fast-forwarded to origin's 7d15d4c9dabda5f4add72ad37bf52d3582e19f6a — no local drift to reconcile, remote already carried the full F0/features-1-14 roadmap implementation (111 files, brain/live.ts, readiness.ts, debrief.ts, trajectory.ts, weekly.ts, detectors, UI surfaces, tests) plus this escobar-presence package itself.
User-visible change: none (verification only).
Owned files and any reconciled baseline drift: docs/escobar-presence/PROGRESS.md only.
Architecture decisions: none.
Regression IDs: none owned by P00.
Focused failure reproduction and result: n/a.
Full commands → exit/result/test counts/skips: `npx tsc --noEmit` (app) exit 0; `npm test` — 54 files, 627 passed; `cd proxy && npx tsc --noEmit` exit 0, `npm test` — 87 passed, 8 skipped (conditional, no live-model credentials, as expected); `npm run build` — succeeded, www/ regenerated.
Browser evidence paths, themes/widths and console/network result: not re-run locally this pass (budget-constrained); relying on exact-head CI evidence below, which already ran the five-theme visual/migration gate and both android/source jobs on this same commit.
Reviewer identity, findings, fixes and re-review: none — no code changed.
Commit and verified remote SHA: pending this commit's push.
Exact-head workflow URL + source/Android conclusions: https://github.com/macdarenz-droid/M-arc/actions/runs/35617178195 — source-gate success, android-gate success (APK built, checksum verified in that run), both for head_sha 7d15d4c9dabda5f4add72ad37bf52d3582e19f6a, which is the exact commit this local worktree is now on.
Known limits / remaining proof: this session did not run `npm run gate` (Playwright five-theme) locally — CI already proved it green on this exact SHA, so it was not re-run to conserve budget. P01–P10 are unimplemented; nothing beyond this package and the already-shipped F0/1–14 roadmap exists in the app yet.
Next concrete action: **hand-off point.** This Claude session stopped here on the owner's stated credit limit. The next agent (any Sonnet-Medium-capable executor) should start P01 (moment selector, tone, shared dismissals) directly from 01-ARCHITECTURE.md and 02-IMPLEMENTATION-WORKFLOW.md's P01 section, following the same loop: implement → review → full local gate → commit → push → verify exact-head CI → append its own ledger record here → continue to P02 without stopping for a new prompt, per this package's own resume protocol. Nothing is blocked; this is a scope/budget stop, not a technical one.
```

```text
Patch / date / executor: P01 / 2026-09-21 / Claude Sonnet 5
Starting local and remote SHA: 3630aee (P00 checkpoint), both local and origin.
User-visible change: none — pure brain/state work only, no UI mounting (per P01's own scope: "Keep network and UI mounting out of this patch").
Owned files and any reconciled baseline drift: NEW src/brain/coach/moments.ts; src/core/models.ts (CoachTone, CoachPresenceDismissal, MAX_PRESENCE_DISMISSALS, CoachPresence types, optional CoachState.presence field); src/core/store.ts (normalizePresence + wiring into normalize()); NEW tests/coach-moments.test.ts, tests/coach-presence.test.ts. No drift to reconcile.
Architecture decisions: (1) selectMoment() ranks candidates built only from the already-rendered Insight[]/Suggestion[] (words.ts) — it does not recompute findings/proposals or scan sessions itself, keeping it a thin, cheap adapter per §2's "Pure cue selection and wording" row. (2) Suggestions always outrank insights (priority floor 1000 vs. insight's existing 0-3xx range) as the concrete reading of §3's "then relevant existing proposal" tier, since P01 has no live-adjustment/objective evidence yet to rank against. (3) coach.presence is a genuinely optional field (undefined, not a materialized default object) so every pre-P01 save round-trips byte-for-byte until the person actually sets a tone or dismisses something. (4) normalizePresence drops invalid dismissal entries individually but keeps a structurally valid presence object (version/tone intact) — read as the more literal form of D03's "drops only invalid data" than voiding the whole object over one bad entry.
Regression IDs → concrete test/scenario names: M01 (tests/coach-moments.test.ts: stable id across tone, identical repeated selection, evidenceKeyFor determinism), M02 (null with no evidence; a lone insight still selects), M03 partial — suggestion-over-insight tier only, no UI slot-dedup yet (that's P02's), M04 (tone changes `cue` only — full field-diff assertion both for an insight and a suggestion), M05 (exact id+evidenceKey match required to suppress; a stale evidenceKey does not suppress a materially changed situation; full suppression yields null), M06 (repeated calls don't mutate input, pure), M07 not touched — no change to existing coach.dismissed/snoozedUntil/accepted paths, existing tests (coach-apply, coach-unfinished, coach-ask-memory) still pass unmodified — PERF01 partial (selector-level O(n) smoke test only; the real call-count assertion on report/history scans is P02/P03's, once a controller boundary exists). D01 (tests/coach-presence.test.ts: absent field loads undefined, fresh install too), D02 (valid presence + dismissals round-trip exactly through save/reload, survives alongside an active session), D03 (six malformed-shape cases + mixed valid/invalid dismissal entries + over-cap retention, sessions/splits untouched in every case).
Focused failure reproduction and result: wrote the 27 new tests before/alongside the implementation; two D03 cases initially asserted the wrong (more destructive) behavior — voiding the whole presence object over one bad dismissal entry — caught by a real assertion failure, then corrected the test to match the intentionally chosen less-destructive per-entry drop (see architecture decision 4 above), not a code bug.
Full commands → exit/result/test counts/skips: `npx tsc --noEmit` (app) exit 0; `npm run typecheck` exit 0; `npx tsc --noEmit -p proxy/tsconfig.json` exit 0; `npm test` — 56 files, 654 passed (627 baseline + 27 new, zero regressions); `npm --prefix proxy test` — 87 passed, 8 skipped (unchanged, conditional on live-model credentials); `npm run build` — succeeded; `MARC_CHROMIUM=/opt/pw-browsers/chromium npm run gate` — PASS, 5 themes, no page errors, legacy import verified.
Browser evidence paths, themes/widths and console/network result: the visual gate above is the full existing scripts/screenshot-gate.mjs run (silent-black, paper, ember, emerald, midnight, all loaded clean) — P01 adds no new UI surface, so no new browser scenario was added to the gate this patch; that starts in P02 per its own "Own: shared launcher/cue/panel... Today, Train, History, Body, Coach" scope.
Reviewer identity, findings, fixes and re-review: self-review only (no second model available in this pass) — this is pure/additive brain+state work (no persistence-journal, grounding-validator or proxy-payload change), so per the workflow's own rule ("Persistence, journal, grounding and action changes require independent review before advancement") an independent reviewer is not mandatory for this specific patch, but is still owed generally; flagging honestly rather than claiming one happened.
Commit and verified remote SHA: pending — recorded in the next patch's entry once pushed and CI is confirmed, per this ledger's own anti-self-reference rule.
Exact-head workflow URL + source/Android conclusions: pending, same reason.
Known limits / remaining proof: M03's UI-slot-dedup half and PERF01's real call-count half are correctly deferred to P02 (their prerequisites — a mounted controller and existing screen slots — don't exist as consumers yet); this patch's own scope is fully covered. No UI, no network call, no session mutation was added, matching P01's exit criteria.
Next concrete action: push this commit, verify exact-head CI (both source-gate and android-gate) on the pushed SHA, record that result at the start of P02's entry, then begin P02 (quiet presence and local explanations across screens) without a new prompt — continuing per the owner's explicit instruction to proceed autonomously through patches with a trace left at each one.
```

```text
Patch / date / executor: P02 (partial — Today only) / 2026-09-21 / Claude Sonnet 5
Starting local and remote SHA: 62d097c (P01, VERIFIED above), both local and origin.
User-visible change: Today's existing "Coach" section — which already held one top insight card plus a "waiting suggestions" link, i.e. the doc's "occupied coaching slot" — now shows the shared presenceMoment (from P01's selectMoment) through the new PresenceLauncher instead of an ad-hoc top-insight card, with an explicit dismiss (×) and a tap-through to the existing InsightSheet/SuggestionSheet detail (now exported, not re-implemented). No second card was added: the prior top-insight card is replaced in place, not stacked alongside.
Owned files and any reconciled baseline drift: NEW src/brain/coach/moments.ts consumer src/app/selectors.ts (added `presenceMoment` computed, excludes today_plan since Today already has its own dedicated accept flow for that); NEW src/slices/coach/presence.ts (dismissPresenceMoment, setPresenceTone — the only writers of coach.presence); NEW src/slices/coach/Presence.tsx (PresenceLauncher, presentational only); src/slices/coach/Coach.tsx (exported the previously-private InsightSheet/SuggestionSheet for reuse — behavior unchanged, only visibility); src/slices/today/Today.tsx (Coach section rewired to the shared moment); NEW tests/coach-presence-ui.test.ts. No drift to reconcile.
Architecture decisions: (1) Reused Today's existing "Coach" Section as the presence surface rather than adding a new one, per §1/§3's "reuse existing coaching slots... never stack a duplicate copy" — this was a real judgment call: the prior `top`/`waiting` logic was itself the occupied slot on this screen. (2) today_plan suggestions stay excluded from presenceMoment's candidate pool since Today has its own prominent, distinct accept-a-plan flow elsewhere on the same screen — showing it twice via two different mechanisms would be the exact duplication the spec forbids. (3) Detail views reuse the exact existing InsightSheet/SuggestionSheet (now exported) rather than new presence-specific sheets, so there is only one rendering of any given insight/suggestion's full detail in the whole app. (4) Scoped this patch to Today only — Train, History, Body, Coach and contextual Settings integration (all also named in P02's "Own") are not yet done; each has its own existing banners/slots (rest banner, PR hints, debrief, chronic-skip cards, etc.) that need the same "which existing slot does this reuse" judgment call Today needed, and rushing that across five more screens in one pass risked exactly the "duplicated cue or modal stacking" P02's Exit criteria forbids. This patch stays IN PROGRESS per the workflow's own "a partial patch stays IN PROGRESS" rule, not falsely marked VERIFIED/done.
Regression IDs → concrete test/scenario names: B01/B05 (Today's presence entry, tests/coach-presence-ui.test.ts's PresenceLauncher render assertions — no moment shows only the quiet entry, not an empty card; a moment shows its cue, not a duplicated verbatim block), M03 (this patch's concrete instance of it: the existing Today insight slot is reused, not duplicated — verified by inspection of the diff, no automated "count Card.insight instances" regression added this pass, noted as a limit below), M06 partial (no network path touched — Today's presence wiring only reads existing computed signals and calls the same acceptProposal/dismissProposal/dismissPresenceMoment functions already covered elsewhere), D01-D03 unchanged from P01 (still covered by tests/coach-presence.test.ts, untouched this patch). B02-B04, B12 (Train/History/Body/Coach surfaces) remain NOT STARTED — correctly not claimed this patch.
Focused failure reproduction and result: n/a — additive UI wiring with existing, already-tested mutation functions (acceptProposal/dismissProposal unchanged) and a new pure presentational component; the 6 new tests in tests/coach-presence-ui.test.ts (3 PresenceLauncher render cases, 3 dismissPresenceMoment/setPresenceTone mutation cases) were written directly against the intended contract and passed once the implementation matched it, no red-then-green cycle needed for this shape of change.
Full commands → exit/result/test counts/skips: `npx tsc --noEmit` (app) exit 0; `npm test` — 57 files, 660 passed (654 from P01 + 6 new this patch); `npm run build` — succeeded; `MARC_CHROMIUM=/opt/pw-browsers/chromium npm run gate` — PASS, 5 themes, no page errors, legacy import verified (Today renders in every theme with the new section, since the gate's fixture data has real insights/suggestions to exercise the moment-present path).
Browser evidence paths, themes/widths and console/network result: existing scripts/screenshot-gate.mjs run above; no new dedicated browser scenario was added for the presence cue's own open/dismiss interaction this pass (that requires either extending the gate script with a new named scenario or a live Playwright click-through, which this session did not have budget to add carefully — flagged honestly below rather than skipped silently).
Reviewer identity, findings, fixes and re-review: self-review only, same as P01 — this patch touches UI/state (not persistence journal, grounding validator or proxy payload), so independent review is owed generally but not gating for advancement per the workflow's own rule; noted honestly.
Commit and verified remote SHA: pending — recorded in the next patch's entry once pushed and CI is confirmed.
Exact-head workflow URL + source/Android conclusions: pending, same reason.
Known limits / remaining proof: (a) Train, History, Body, Coach and contextual Settings still need the same launcher wired to their own occupied slot (or a clearly-justified new slot where none exists) — each needs its own "what does this screen already show that would duplicate" read, the same care Today got; (b) no interactive (click-through) browser scenario for open/dismiss was added to the gate script this pass — the gate proves no page errors and correct theme rendering with the section present, not that tapping the cue opens the right sheet or that dismiss removes it, though tests/coach-presence-ui.test.ts covers that logic at the unit level; (c) zero-external-request verification (M06's browser half) for the presence surface specifically was not separately instrumented — Today's screen already has no network calls in its existing render path and nothing new here adds one, but this is inspection, not an automated network-recorder assertion.
Next concrete action: push this commit, verify exact-head CI (both jobs) on the pushed SHA, record that result at the start of the next entry, then continue P02 on Train next (it already has a rest banner and other in-workout cues — read those first, the same way Today's existing Coach section was read here, before adding anything). Do not restart Today's work; it is done for this pass. If a different executor picks this up, they can start directly from "Train" without re-reading Today's diff beyond this record.
```

```text
Patch / date / executor: P02 Train attempt — REVERTED, no commit / 2026-09-21 / Claude Sonnet 5
Starting local and remote SHA: b54784d (P02 Today, VERIFIED above), both local and origin.
User-visible change: none — reverted before commit; working tree is byte-identical to b54784d.
Owned files and any reconciled baseline drift: attempted src/slices/workout/Train.tsx (Splits/pre-workout header only — deliberately left LiveSession untouched) and tests/coach-presence-ui.test.ts. Both fully reverted with `git checkout --`; confirmed `git diff --stat` empty before moving on.
Architecture decisions: gated the launcher on `!remoteEnabled.value` so it never doubles up with the existing online "Ask Escobar"/"Import" buttons in the same topbar row (those only show when `remoteEnabled.value` is true) — a real, sound design call, orthogonal to the bug found below. Scoped strictly to the pre-workout Splits() screen, leaving LiveSession (rest banner, live-adjustment offers, warm-up ramp, etc.) completely alone, per P02's "Keep active set fields, rest banner and destructive confirmations in control."
Regression IDs → concrete test/scenario names: none landed — this patch never reached a committed state.
Focused failure reproduction and result: **found a real, reproducible bug, not shipped.** With the PresenceLauncher card added to Train's Splits() topbar `.row` (alongside the existing "Split" button), `MARC_CHROMIUM=/opt/pw-browsers/chromium npm run gate` fails deterministically (reproduced twice) in the "silent-black effort repair" scenario: after finishing a workout, rating effort, reloading, the very next line — clicking the bottom nav's "Today" button — times out with Playwright reporting `<div class="app">…</div> intercepts pointer events` at the button's location, even though the element resolves, is "visible, enabled and stable" by Playwright's own check. Diagnostic instrumentation (added temporarily to a local, uncommitted copy of scripts/screenshot-gate.mjs, never committed) showed: no thrown JS exception (`pageerror`/`console` listeners silent), the nav and button ARE present with normal geometry and `pointer-events: auto`, and `document.elementFromPoint` at the button's own center correctly resolves to the button's own icon in a static snapshot taken immediately before the click — yet Playwright's own live hit-test at click time still fails identically. This points to something timing/render-cycle-related (possibly the long readiness-derived cue text — "About 0% recovered with about 2d to go..." — causing a reflow or transition exactly around click time) rather than a static layout bug, but it was **not fully root-caused** before time was better spent reverting cleanly than continuing to chase it. Isolated with high confidence to this specific change: stashing only Train.tsx (keeping Today's already-verified diff) made the gate pass again immediately; restoring it reproduced the failure again immediately. Today's own integration was re-verified green afterward (see Full commands below) to confirm the revert was complete and nothing else regressed.
Full commands → exit/result/test counts/skips: after reverting, `npx tsc --noEmit` exit 0; `git diff --stat` empty (confirms full revert); `npm run build` succeeded; `MARC_CHROMIUM=/opt/pw-browsers/chromium npm run gate` — PASS, 5 themes, no page errors (re-confirms the b54784d baseline this session already verified is still intact).
Browser evidence paths, themes/widths and console/network result: n/a — no new surface shipped.
Reviewer identity, findings, fixes and re-review: self-caught via the mandatory local gate, exactly as the workflow's own step 5 ("Verify... Fix failures without asking the owner to request regression") requires — the difference here is the fix chosen was "don't ship this yet" rather than a patch, since the root cause wasn't nailed down with confidence in the time available.
Commit and verified remote SHA: none — nothing pushed for this attempt. HEAD is still b54784d.
Exact-head workflow URL + source/Android conclusions: n/a — see P02 Today's own entry above, re-confirmed green by this entry's revert-verification step.
Known limits / remaining proof: **do not re-attempt a Train presence launcher by simply re-adding the same Card-in-topbar-row pattern without first understanding this bug.** Concrete leads for the next attempt: (1) try a materially shorter/truncated cue string in the topbar context (the readiness insight's cue text was unusually long — "About 0% recovered with about 2d to go, after Mon, Sep 21. Your check-in today read low, which widens it a little more." — Today's own version of the same card never hit this because Today's Section layout gives it a full-width block context instead of a cramped flex `.row` alongside other buttons); (2) check whether `.topbar .row`'s flex sizing interacts badly with `.card`'s default width/flex-basis when a sibling item's content is long enough to wrap to multiple lines; (3) reproduce with Playwright's own trace viewer (`PWDEBUG=1` or `--trace on`) rather than manual `elementFromPoint` snapshots, which clearly weren't catching the actual state at click time; (4) consider constraining PresenceLauncher itself (e.g. a max-width/line-clamp in its own component, or a dedicated slot below the topbar rather than inside its button row) so it's reusable in tight header contexts, then re-verify Train before the other four screens, which likely have a mix of tight-header and full-width-section contexts and should each be checked against this same failure mode once it's understood.
Next concrete action: before touching Train again, reproduce this failure with Playwright tracing enabled to see the actual frame-by-frame layout at click time (not a static snapshot), form a real hypothesis, fix it, and add a regression scenario to the gate script itself proving nav stays clickable with a long-cue presence card present in a tight header — only then re-add the Splits() integration, run the full local gate at least twice to confirm it's not flaky, and continue to History next (afterward the intent is to move through Body/Coach/Settings, but re-derive the actual order from current state rather than assuming it, since this entry may not be the most recent one by the time it's read).
```

```text
Patch / date / executor: PresenceLauncher hardening (truncation) / 2026-09-21 / Claude Sonnet 5
Starting local and remote SHA: c694a0d (P02 Train-attempt revert record, VERIFIED above), both local and origin.
User-visible change: PresenceLauncher's cue text is now bounded — `overflow: hidden; text-overflow: ellipsis; white-space: nowrap` on the cue span, with `minWidth: 0; flex: 1 1 auto; overflow: hidden` on the Card and `flexShrink: 0` on the icon/dismiss button — instead of the previous unconstrained `class="wrap small"` span that could grow to any width. The full cue remains in the `aria-label` regardless of visual truncation. Applied to the shared component, so Today's already-shipped presence card in the "Coach" Section also picks this up (a strict robustness improvement there, not a behavior change — Today's full-width Section never needed truncation, but bounding it costs nothing and makes the component actually safe to reuse in any future tight container).
Owned files and any reconciled baseline drift: src/slices/coach/Presence.tsx only. No drift.
Architecture decisions: this was written and tested specifically to (dis)prove the leading hypothesis from the prior reverted Train entry — that the Train nav-click-interception bug was caused by the readiness insight's unusually long, unconstrained cue text breaking the topbar's flex layout. **The hypothesis was refuted.** Re-adding the Train integration with this exact truncation fix applied (tested locally, then reverted again — see below) still failed the visual gate, but at a *different* point (the "History" nav click in an earlier scenario, not the "Today" click after effort-repair) — proving the failure isn't specific to one long string or one scenario's data, but reproduces generally whenever the PresenceLauncher's `Card` (a `<div role="button" tabIndex="0">` that itself contains a real, nested `<button>` for dismiss) sits in Train's topbar `.row`, next to the bottom-fixed `.nav`. Keeping this hardening anyway: bounding cue text is correct defensive UI regardless of the Train bug's real cause, and it was fully verified safe for Today (full suite, typecheck, build, 5-theme gate all green) before being kept as its own small commit.
Regression IDs → concrete test/scenario names: existing tests/coach-presence-ui.test.ts PresenceLauncher assertions (text still present, just now wrapped in a nested truncating span) continue to pass unmodified — no new test added specifically for truncation since it's a pure CSS/layout change with no new branching logic to cover; visually confirmed via the 5-theme gate screenshots (no overflow warnings, no page errors).
Focused failure reproduction and result: reproduced the Train bug a second time with this fix applied (`npm run build && MARC_CHROMIUM=... npm run gate`), confirming it is NOT a text-length issue — failed at scripts/screenshot-gate.mjs:249 (History nav click) instead of :416 (Today nav click after effort-repair), i.e. a different scenario and location, same "`<div class=\"app\">` intercepts pointer events" signature. Reverted the Train.tsx side of that attempt again (`git checkout -- src/slices/workout/Train.tsx`); kept only this Presence.tsx hardening, re-verified clean (`git diff --stat` showed only Presence.tsx) and green.
Full commands → exit/result/test counts/skips: `npx tsc --noEmit` exit 0; `npm test` — 57 files, 660 passed (no new tests, no regressions); `npm run build` succeeded; `MARC_CHROMIUM=/opt/pw-browsers/chromium npm run gate` — PASS, 5 themes, no page errors (this is with Presence.tsx's truncation applied and Train.tsx back to its unmodified b54784d state).
Browser evidence paths, themes/widths and console/network result: existing gate run above; no dedicated new scenario for truncation itself.
Reviewer identity, findings, fixes and re-review: self-review; this is a small, low-risk, easily-verified CSS-only change to an already-tested component.
Commit and verified remote SHA: pending — recorded in the next entry once pushed and CI is confirmed.
Exact-head workflow URL + source/Android conclusions: pending, same reason.
Known limits / remaining proof: **the Train nav-click-interception bug is real, reproduces at ≥2 distinct points, and remains unsolved.** Refined diagnosis for the next attempt: the likely culprit is the PresenceLauncher `Card`'s structure itself — a `<div role="button" tabIndex="0">` (Card's own pattern for `onClick`) containing a literal nested `<button>` (the dismiss control) — sitting inside Train's `.topbar .row`, a flex row immediately followed later in the DOM by the `position: fixed` bottom `.nav` (z-index 20, confirmed via styles.css). This exact nested-interactive pattern (Card containing real Buttons) already ships successfully elsewhere (Coach.tsx's suggestion list), so the pattern alone isn't inherently broken — something specific to *Train's topbar context* combined with it is. Do not re-attempt by guessing further; use Playwright's trace viewer (`npx playwright show-trace` after running the gate script with tracing enabled via `context.tracing.start(...)`/`stop(...)` around the failing repairCtx/skipCtx, or simpler: temporarily run just that one scenario standalone with `--debug`) to see the actual frame-by-frame state at the failed click, rather than static `elementFromPoint` snapshots (which this session tried and which misleadingly showed everything looked fine moments before the real failure).
Next concrete action: leave Train integration for a future patch with real Playwright trace tooling available. Proceed to History next for P02 (it has its own existing banners — Records chip, volume/exercise-progress sections — read those first). Before wiring History's launcher, sanity-check it in isolation with the full gate immediately, the same way this session caught the Train bug early rather than late.
```

```text
Patch / date / executor: P02 Train — ROOT-CAUSED AND FIXED / 2026-09-21 / Claude Sonnet 5
Starting local and remote SHA: b53b068 (PresenceLauncher truncation hardening, VERIFIED above), both local and origin.
User-visible change: Train's Splits (pre-workout) screen now shows the shared presence cue, in its own full-width row directly below the topbar — not inside the topbar's compact icon-button row. Gated on the online coach being off (mutually exclusive with the existing "Ask Escobar"/"Import" buttons, which only show when it's on). Tapping it opens the exact same InsightSheet/SuggestionSheet used everywhere else; an explicit × dismisses through the same coach.presence writer Today already uses.
Owned files and any reconciled baseline drift: src/slices/workout/Train.tsx (Splits() only — LiveSession untouched); src/slices/coach/Presence.tsx (removed white-space:nowrap/text-overflow:ellipsis from the cue span — see root cause below; this also changes Today's already-shipped rendering from single-line-truncated to multi-line-wrapped, verified safe by the same gate runs); tests/coach-presence-ui.test.ts (+5 tests). No drift to reconcile.
Architecture decisions: root-caused via systematic bisection rather than another guess (full trail below) — the fix is two independent, now-verified findings, not one: (1) never single-line-truncate this cue's text with the nowrap+ellipsis combo, ever, in any container — codified as a permanent regression test; (2) never place this launcher inside a cramped shared flex row alongside other compact buttons — codified as a source-check test that it sits in its own row. Both are now enforced by tests, not just comments, so a future edit that reintroduces either can't silently regress.
Regression IDs → concrete test/scenario names: M03 (Train's own occupied-slot judgment — its own full-width row, mutually exclusive with the online Ask entry, verified by source-check tests); B01-B05 general presence coverage extended to a second surface; the new truncation regression test stands in for a lightweight always-on PERF/layout guard the regression matrix doesn't explicitly name. Full IDs remain the responsibility of P10's integrated pass.
Focused failure reproduction and result — the full diagnosis trail (kept in detail; this is the one worth reading before touching this component again):
  1. First attempt (this session, earlier): PresenceLauncher (Card, role=button, nested dismiss <button>, single-line-truncated cue) placed inside `.topbar .row` alongside the Ask/Import/Split buttons. Visual gate failed deterministically: bottom nav's "Today" button became unclickable ("`<div class=\"app\">` intercepts pointer events") in the effort-repair scenario. Reverted (commit c694a0d).
  2. Second attempt: added cue-text truncation to Presence.tsx as a hypothesis fix, re-tried Train. Gate still failed — same signature, different scenario/click (History nav instead of Today). This refuted "text length" as the (sole) cause and proved the failure isn't scenario-specific. Reverted the Train side, kept the truncation as independent hardening (commit b53b068).
  3. This session: added temporary diagnostic instrumentation directly to a local, never-committed copy of scripts/screenshot-gate.mjs — a screenshot + full DOM dump + computed-style probe taken at the exact moment of the failed click. The screenshot showed the UI rendering *correctly* (no visible overlay, no broken layout); the DOM probe's oversized nav rect was a self-inflicted artifact of taking a `fullPage: true` screenshot immediately before the probe (that mode temporarily resizes the effective viewport for capture), not a real state — an important negative result that stopped a wrong lead.
  4. Systematic bisection followed, each step rebuilt and re-run against the full five-theme gate: plain `<span>` in Train's topbar → PASS. The real `PresenceLauncher` component (identical visible output) → FAIL. Ruled out: being a separate component function (inlining the identical JSX still failed); the interactive role/tabIndex from Card's onClick (a Card with onClick, no nested button, no truncation → PASS); the nested dismiss `<button>` alone (present without truncation styling → PASS); `stopPropagation()` on the dismiss handler (removing it did not change the outcome). Isolated to two independent culprits: (a) `white-space: nowrap` alone on the cue span (confirmed sufficient to fail, with or without the ellipsis/overflow properties alongside it) — this specific ~140-character readiness cue's unwrapped intrinsic width exceeds the viewport in a cramped flex row, and under headless Chromium's `isMobile + hasTouch` emulation that real horizontal overflow decouples the layout viewport from the visual one, breaking touch/click hit-testing on unrelated fixed-position elements elsewhere on the page (the bottom nav) — a genuine, reproducible browser-emulation interaction, not a logic bug in this app's code; (b) independently, even with wrapping (non-nowrap) text, the nested dismiss `<button>` sitting inside the SAME cramped `.topbar .row` (shared with the Ask/Import/Split buttons) still failed — moving the whole launcher out of that row into its own full-width block (matching the existing "easier week" banner's own placement pattern in the same file) fixed this second cause independently of the first.
  5. Final fix (both causes addressed) re-run against the full gate **8 times in a row** (4 immediately after the fix, 4 more after adding regression tests and rebuilding) — all 8 passed cleanly. No flakiness observed at any point once both causes were fixed; every prior "fixed" attempt that still had one of the two causes present failed **every single time**, deterministically — this was never a flaky/intermittent bug.
Full commands → exit/result/test counts/skips: `npx tsc --noEmit` (app) exit 0; `npm run typecheck` exit 0; `npx tsc --noEmit -p proxy/tsconfig.json` exit 0; `npm test` — 57 files, 665 passed (660 + 5 new: 1 truncation-regression test on PresenceLauncher, 4 Train source-check tests); `npm --prefix proxy test` — 87 passed, 8 skipped (unchanged); `npm run build` succeeded; `MARC_CHROMIUM=/opt/pw-browsers/chromium npm run gate` — PASS ×8 consecutive runs, 5 themes each, no page errors, legacy import verified every time.
Browser evidence paths, themes/widths and console/network result: the 8 gate runs above are the full existing five-theme + legacy-import + effort-repair + chronic-skip + swap + debrief + reach scenario suite, all passing with Train's new surface present and exercised (the effort-repair and chronic-skip scenarios specifically visit Train's Splits() screen mid-flow, which is exactly where this launcher renders).
Reviewer identity, findings, fixes and re-review: self-review, but unusually rigorous for this specific patch given the failure mode: every intermediate hypothesis was tested against the *real* full gate rather than assumed fixed, including two negative results (the `fullPage` screenshot artifact, and `stopPropagation` not mattering) that are recorded above specifically so a future reviewer doesn't re-waste time on either.
Commit and verified remote SHA: pending — recorded in the next patch's entry once pushed and CI is confirmed.
Exact-head workflow URL + source/Android conclusions: pending, same reason.
Known limits / remaining proof: this fixes Train specifically. History, Body, Coach and contextual Settings integration remain P02's open scope. Given finding #4(b) above, any future screen's integration should default to "own row, never inside an existing tight icon-button row" unless that screen's own layout is proven wide enough (Today's full-width Section already was, which is why it never hit this). Given finding #4(a), the "never single-line-truncate a long dynamic cue" rule is now permanent and enforced by test — any future PresenceLauncher variant or copy of its pattern must keep that test passing.
Next concrete action: push this commit, verify exact-head CI (both jobs) on the pushed SHA, record that result at the start of the next entry, then continue P02 on History next, applying both lessons from this entry (own row if History's existing layout is tight anywhere near a fixed/sticky element; no nowrap/ellipsis truncation of dynamic cue text) from the start rather than rediscovering them.
```

```text
Patch / date / executor: P02 History / 2026-09-21 / Claude Sonnet 5
Starting local and remote SHA: e94663f (Train root-cause fix, VERIFIED above), both local and origin.
User-visible change: History (both the Log and Stats segments — one mount point above the segment switch, so it can't duplicate or drop when switching) shows the shared presence cue in its own full-width row below the topbar. History had no existing "Ask"/coaching entry point at all, so this is a genuinely new slot, not a reused one — nothing to duplicate.
Owned files and any reconciled baseline drift: src/slices/history/History.tsx only; tests/coach-presence-ui.test.ts (+3 tests). No drift.
Architecture decisions: applied both lessons from the Train investigation from the start rather than rediscovering them: (1) own full-width row, never a shared button row (History's topbar happens to have no button row at all, but the same placement was kept anyway for consistency with Train/Today and because it's the verified-safe shape); (2) PresenceLauncher's cue already wraps instead of truncating (fixed upstream in the same component Train uses, nothing extra needed here). Mounted at the top-level History() component, not inside Log()/Stats(), so the same instance persists across the segment toggle.
Regression IDs → concrete test/scenario names: same coverage class as Train (M03 own-slot judgment, B01-B05 general presence). New source-check tests confirm: launcher outside `.topbar`; InsightSheet/SuggestionSheet reused (exactly one of each in the file); mounted once above the Log/Stats branch, not per-segment.
Focused failure reproduction and result: n/a — no failure this time. Applying the Train lessons proactively meant the full gate passed on the first attempt, run 4 times in a row for confidence given the precision bar this session is holding to, rather than assuming one green run is enough.
Full commands → exit/result/test counts/skips: `npx tsc --noEmit` (app) exit 0; `npm run typecheck` exit 0; `npx tsc --noEmit -p proxy/tsconfig.json` exit 0; `npm test` — 57 files, 668 passed (665 + 3 new); `npm --prefix proxy test` — 87 passed, 8 skipped (unchanged); `npm run build` succeeded; `MARC_CHROMIUM=/opt/pw-browsers/chromium npm run gate` — PASS ×4 consecutive runs, 5 themes each, no page errors.
Browser evidence paths, themes/widths and console/network result: the 4 gate runs above are the full existing scenario suite; History's Log/Stats screens are visited directly in the standard per-theme walk (`history`/`stats` screenshots), now with this new surface present.
Reviewer identity, findings, fixes and re-review: self-review; this patch is low-risk relative to Train's (new empty slot, no competing layout, both hard lessons already known and applied), so the multiple gate runs were about honoring the precision bar generally, not chasing a specific suspicion.
Commit and verified remote SHA: pending — recorded in the next patch's entry once pushed and CI is confirmed.
Exact-head workflow URL + source/Android conclusions: pending, same reason.
Known limits / remaining proof: Body, Coach and contextual Settings integration remain P02's open scope. Coach already has its own full insight/suggestion list UI (the "destination" screen the other surfaces' sheets and "All"/"Review all" links point to) — worth deciding explicitly whether Coach needs its own presence launcher at all, or whether it's correctly exempt as the one screen where the full list already *is* the presence surface, before wiring it reflexively.
Next concrete action: push this commit, verify exact-head CI (both jobs) on the pushed SHA, record that result at the start of the next entry, then decide Coach's status explicon (likely: exempt, with a one-line note why) before moving to Body and then contextual Settings.
```

```text
Patch / date / executor: P02 Body / 2026-09-21 / Claude Sonnet 5
Starting local and remote SHA: 591a90b (History, VERIFIED above), both local and origin.
User-visible change: Body shows the shared presence cue in its own full-width row below the topbar, same verified-safe shape as History (Body's topbar also has no button row to share). Present across all three Body view segments (Recovery/This week/Levels) from one mount point.
Owned files and any reconciled baseline drift: src/slices/body/Body.tsx only; tests/coach-presence-ui.test.ts (+2 tests). No drift.
Architecture decisions: identical shape to History — own row, wrapped (not truncated) cue, `!remoteEnabled.value` gate for consistency even though Body has no existing Ask entry to actually collide with (kept for uniformity across screens, so a future global online-Ask entry point doesn't have to retrofit this gate everywhere).
Regression IDs → concrete test/scenario names: same class as Today/Train/History. Source-check tests: launcher outside `.topbar`; InsightSheet/SuggestionSheet reused (exactly one of each).
Focused failure reproduction and result: n/a — third consecutive screen where applying the two Train lessons from the start meant the gate passed clean on the first attempt.
Full commands → exit/result/test counts/skips: `npx tsc --noEmit` exit 0; `npm run typecheck` exit 0; `npx tsc --noEmit -p proxy/tsconfig.json` exit 0; `npm test` — 57 files, 670 passed (668 + 2 new); `npm --prefix proxy test` — 87 passed, 8 skipped (unchanged); `npm run build` succeeded; `MARC_CHROMIUM=/opt/pw-browsers/chromium npm run gate` — PASS ×4 consecutive runs.
Browser evidence paths, themes/widths and console/network result: existing per-theme `body` screenshot scenario now includes this surface; all 4 runs clean, no page errors.
Reviewer identity, findings, fixes and re-review: self-review; low-risk, same verified shape as the prior two screens.
Commit and verified remote SHA: pending — recorded in the next patch's entry once pushed and CI is confirmed.
Exact-head workflow URL + source/Android conclusions: pending, same reason.
Known limits / remaining proof: Coach and contextual Settings remain P02's open scope. Coach's status still needs an explicit decision (see below) before treating it as done or skipped.
Next concrete action: push this commit, verify exact-head CI, then decide Coach explicitly: Coach.tsx already renders the full ranked insights/suggestions list (the same "All"/"Review all" destination Today and other screens link to) — the presence selector's single top-ranked moment is very likely already visible at or near the top of that existing list, making a second, separate launcher on Coach redundant by construction, not merely stylistically similar. Recommend documenting Coach as **intentionally exempt** with that one-line reasoning, rather than adding a launcher there, then move to contextual Settings (P02's last named surface) to close out the patch.
```

```text
Patch / date / executor: P02 Coach — VERIFIED EXEMPT / 2026-09-21 / Claude Sonnet 5
Starting local and remote SHA: 94e4100 (Body, VERIFIED above), both local and origin.
User-visible change: none — Coach.tsx gains a one paragraph doc comment above its `Coach()` export explaining the exemption; no runtime code changed.
Owned files and any reconciled baseline drift: src/slices/coach/Coach.tsx (comment only); tests/coach-presence-ui.test.ts (+1 test). No drift.
Architecture decisions: verified, not just asserted, before writing anything down — read Coach.tsx's actual `list`/`open` construction (src/slices/coach/Coach.tsx:40-47): `list` is `shortlist(insights.value, 6, 2)` plus up to 2 reviewInTrain items, `open` is `suggestions.value` (snoozed filtered), both sorted by the same `priority` field `presenceMoment`'s selector ranks over. Since `selectMoment` (src/brain/coach/moments.ts) draws its candidates from these exact same `insights.value`/`suggestions.value` arrays and picks the single highest-priority one, that pick is structurally guaranteed — not just usually true — to already be the first card in Coach's own "Suggestions" or "Insights" section. A launcher here would be a literal duplicate of the top existing card, the one failure mode every other presence surface exists to avoid. This is the one screen in P02's named list that is correctly exempt, not skipped.
Regression IDs → concrete test/scenario names: M03 (the general "reuse, never duplicate" rule) — new test asserts Coach.tsx never imports PresenceLauncher AND still reads the same selectors array the reasoning depends on, so if a future refactor changes what Coach reads, the test fails and forces re-reading this reasoning rather than silently going stale.
Focused failure reproduction and result: n/a — no behavior change.
Full commands → exit/result/test counts/skips: `npx tsc --noEmit` exit 0; `npm test` — 57 files, 671 passed (670 + 1 new); `npm run build` succeeded; `MARC_CHROMIUM=/opt/pw-browsers/chromium npm run gate` — PASS, 5 themes, no page errors.
Browser evidence paths, themes/widths and console/network result: existing `coach` per-theme screenshot scenario, unchanged rendering (comment-only diff), still passes.
Reviewer identity, findings, fixes and re-review: self-review; zero-risk (comment + test only).
Commit and verified remote SHA: pending — recorded in the next patch's entry once pushed and CI is confirmed.
Exact-head workflow URL + source/Android conclusions: pending, same reason.
Known limits / remaining proof: contextual Settings is the last named P02 surface. P02's own "Own" line names it as "contextual Settings integration" specifically — worth reading what Settings.tsx currently has (if anything coaching-related) before assuming it needs the same launcher pattern; a settings screen's needs may differ from the four content screens done so far (e.g. a static explanatory link rather than a live per-visit cue).
Next concrete action: push this commit, verify exact-head CI, then read src/slices/settings/Settings.tsx before deciding its integration shape — do not assume the same pattern applies without checking, the way this entry checked Coach rather than assuming it needed a launcher.
```

```text
Patch / date / executor: P02 Settings — VERIFIED EXEMPT (P02's last named surface) / 2026-09-21 / Claude Sonnet 5
Starting local and remote SHA: 0c3cbee (Coach exemption, LOCAL VERIFIED / CI PENDING above), both local and origin.
User-visible change: none — Settings.tsx gains a doc comment above `Settings()`; no runtime code changed.
Owned files and any reconciled baseline drift: src/slices/settings/Settings.tsx (comment only); tests/coach-presence-ui.test.ts (+1 test). No drift.
Architecture decisions: read the full file (all ~130 lines, every Section) before deciding, the same discipline as Coach's exemption. Settings is a pure preferences Sheet — theme, units, reminders, haptics, profile, health sync, the online-coach toggle, backup/restore, data reset. It imports nothing from `@/app/selectors` at all: no `insights`, no `suggestions`, no `report`. The presence selector's moments are built entirely from those report-derived arrays (a plateau, a readiness reading, a suggested target) — Settings has no such content, so there is nothing for a cue to be *about*. This is a different reason than Coach's exemption (duplication) — a category mismatch, not a redundancy — and is recorded as such rather than conflated with Coach's case. Explicitly scoped narrow: this is about the existing PresenceLauncher/moment-cue pattern only. A future settings-specific coaching idea (e.g. "the online coach has been off for 3 weeks") would be a new kind of moment with its own selector logic, not this one reused here — the exemption doesn't foreclose that.
Regression IDs → concrete test/scenario names: source-check test confirms no PresenceLauncher import/usage AND (checked against the actual `@/app/selectors` import line, not naive substring match, since the reasoning comment itself names `insights.value`/`suggestions.value` in prose) that Settings imports none of insights/suggestions/presenceMoment from selectors — so an edit that adds any of them fails the test and forces a re-read of the reasoning rather than silently invalidating it.
Focused failure reproduction and result: n/a — no behavior change. One real test-authoring catch worth recording: the first version of this test naively checked the whole file for the substring "insights.value", which failed immediately because the explanatory comment itself uses that exact phrase in prose — fixed by checking only the actual `@/app/selectors` import line.
Full commands → exit/result/test counts/skips: `npx tsc --noEmit` exit 0; `npm test` — 57 files, 672 passed (671 + 1 new); `npm run build` succeeded; `MARC_CHROMIUM=/opt/pw-browsers/chromium npm run gate` — PASS, 5 themes, no page errors.
Browser evidence paths, themes/widths and console/network result: existing `settings` per-theme screenshot scenario, unchanged rendering (comment-only diff), still passes.
Reviewer identity, findings, fixes and re-review: self-review; zero-risk (comment + test only), same as Coach's entry.
Commit and verified remote SHA: pending — recorded in the next entry once pushed and CI is confirmed. Awaiting confirmation that the Coach-exemption commit (0c3cbee) is CI-green before this entry's commit is pushed, per the workflow's own sequencing rule.
Exact-head workflow URL + source/Android conclusions: pending, same reason.
Known limits / remaining proof: **P02's six named surfaces (Today, Train, History, Body, Coach, contextual Settings) are all now addressed** — four real integrations plus two verified, documented, test-locked exemptions. This is NOT the same as P02 being fully done per its own Exit criteria: "no duplicated cue or modal stacking" across the whole app is claimed here with real verification (each surface checked individually, four gate-run batches totaling 20+ clean passes across this patch alone), but P02's exit also implicitly assumes the broader UI-mounting work its own architecture doc describes (§4's shared contextual explanation/Ask controller) is out of scope — that's explicitly P03's "One shared optional Ask conversation," not P02's. Do not conflate "all six named surfaces addressed" with "P03 already done."
Next concrete action: push this commit, verify exact-head CI, then mark P02 VERIFIED in the status table (not just this entry) and move to P03 (shared guarded contextual Ask) — read 02-IMPLEMENTATION-WORKFLOW.md's P03 section and 01-ARCHITECTURE.md §4 fresh before starting, since P03 is a materially different kind of work (unifying an existing multi-mount Ask host, request-generation invalidation, remote-enabled guards) than P02's read-only local cue surfacing.
```

```text
Patch / date / executor: P03.1 — in-handler remote-enabled guard / 2026-09-21 / Claude Sonnet 5
Starting local and remote SHA: 3eb600d (P02 VERIFIED above), both local and origin.
User-visible change: AskSheet's composer now shows "Online coach is off. Turn it on in Settings to ask a question — saved drafts above still work." instead of a live input whenever `remoteEnabled` is false, mirroring the existing `savedOnly` message. `send()` itself now refuses (sets the same message as an error) if remote is off, checked fresh at call time rather than trusted from the caller.
Owned files and any reconciled baseline drift: src/slices/coach/AskSheet.tsx only (App/controller unification is a later P03 sub-patch, not this one); NEW tests/ask-sheet-remote-guard.test.ts. No drift.
Architecture decisions: split P03 into sub-patches, same discipline as P02 — this is the one piece 01-ARCHITECTURE.md §4 calls "Critical" and flags as independently necessary regardless of the rest of the host-unification work: "the current AskSheet.send relies on Coach's remote-enabled mounting. A global host MUST check the current remoteEnabled inside the send handler as well as disabling the composer." Shipping it now, standalone, closes a real gap today (both existing mount points — Coach.tsx's askOpen and Train.tsx's buildingSplit — only gate the *button that opens* the sheet on remoteEnabled, never re-check it once the sheet is already mounted) rather than waiting for the larger controller refactor. Chose to disable the composer AND guard send() (defense in depth) rather than either alone, matching the doc's explicit "as well as" wording.
Regression IDs → concrete test/scenario names: A02 partial (the handler-level refusal is now real and source-verified; the specific "flip remote off while sheet is already open" browser race is honestly deferred — see Known limits). New tests/ask-sheet-remote-guard.test.ts: guard appears before any payload-building call in send(); composer is fully replaced (not just visually disabled) when remote is off; savedOnly's offline draft-review actions (SplitDraftAction/ScheduleDraftAction/GoalChangeAction) never reference remoteEnabled, confirming the guard scopes to the composer/send path only, not the whole sheet.
Focused failure reproduction and result: n/a — additive guard, no prior failing case to reproduce (the gap was "nothing checks," not "something checks wrong").
Full commands → exit/result/test counts/skips: `npx tsc --noEmit` exit 0; `npm run typecheck` exit 0; `npx tsc --noEmit -p proxy/tsconfig.json` exit 0; `npm test` — 58 files, 675 passed (672 + 3 new); `npm --prefix proxy test` — 87 passed, 8 skipped (unchanged); `npm run build` succeeded; `MARC_CHROMIUM=/opt/pw-browsers/chromium npm run gate` — PASS ×3 consecutive runs (confirms the happy path — remote already on, Ask opened from Coach/Train, sent — is unaffected by the new guard).
Browser evidence paths, themes/widths and console/network result: the 3 gate runs above exercise the existing Coach/Train Ask flows with remote on; no new page errors.
Reviewer identity, findings, fixes and re-review: self-review; narrow, additive, source-verified change.
Commit and verified remote SHA: pending — recorded in the next entry once pushed and CI is confirmed.
Exact-head workflow URL + source/Android conclusions: pending, same reason.
Known limits / remaining proof: the specific live-browser race this guard defends against — opening Ask, then flipping the online-coach setting off (currently only reachable by stacking Settings over an already-open Ask sheet, since neither mount point's own launcher button is reachable once remote is off) — is not yet exercised by an automated browser scenario. It's source-verified (the check exists, runs first, before the network call) but not behaviorally verified end-to-end. Deferred honestly to the broader P03 controller patch, where the shared host's own lifecycle work naturally needs exactly this scenario built out; not silently skipped. Full P03 scope remains open: unifying Coach.tsx's and Train.tsx's separate AskSheet mounts into one App-level controller, context-by-IDs, request-generation invalidation on clear/reset/restore, contextual prefill from a presence launcher's "Ask about this" (not yet wired to any of the 4 integrated screens), no-nested-modal enforcement.
Next concrete action: push this commit, verify exact-head CI, then start the actual host-unification piece: design the shared controller (surface/selected-IDs, open state, transient draft, request generation counter) as its own new module, replace Coach.tsx's and Train.tsx's separate `askOpen`/`buildingSplit` local state with it, and add the deferred browser scenario (open Ask, stack Settings, flip remote off, return to Ask, confirm composer is now the static message and a stale send is refused) once a single shared mount makes that scenario straightforward to drive.
```

```text
Patch / date / executor: P03.2 — request-generation invalidation / 2026-09-21 / Claude Sonnet 5
Starting local and remote SHA: e91d653 (P03.1 remote-enabled guard, VERIFIED CI-green above), both local and origin.
User-visible change: none under normal use. A reply that arrives after its conversation was cleared, reset via Settings' "Ask Escobar" reset, or wiped by "Reset everything"/"Restore backup" while the request was still in flight is now silently dropped instead of being appended to whatever now-different (or empty) thread is showing.
Owned files and any reconciled baseline drift: NEW exported `askRequestGeneration`/`invalidateAskRequests` in src/slices/coach/askMemory.ts; src/slices/coach/AskSheet.tsx (capture + re-check around the network call; "Clear conversation" bumps it); src/slices/settings/Settings.tsx (all three reset/restore call sites bump it, immediately before their state-changing call); tests/coach-ask-memory.test.ts (+2), tests/ask-sheet-remote-guard.test.ts (+4, and broadened beyond just the remote-enabled guard — kept in the same file since both are AskSheet-lifecycle source-check tests, not worth splitting). No drift.
Architecture decisions: a plain `signal(0)`, not part of `CoachState` — this describes in-memory request identity ("is the request I started still meaningful"), not anything worth persisting or included in a backup. Deliberately NOT threaded through the pure `askMemory.ts` functions themselves (`clearAskThread`, `clearAskMemory` stay pure, CoachState-in-CoachState-out) — every call site explicitly calls `invalidateAskRequests()` alongside its existing state call, matching how those call sites already explicitly call `update`/`flushSave`/`replaceState` themselves rather than a pure helper doing it. `finally { setSending(false); }` intentionally runs regardless of generation — if the same AskSheet instance is still mounted (user cleared the conversation without closing the sheet), the composer must not stay stuck showing "Thinking..." forever; only the actual state-mutating success path is generation-gated.
Regression IDs → concrete test/scenario names: A05 (the exact requirement: "old success/error/finally cannot modify replacement conversation or newer sending state") — covered by source-check tests confirming capture-before-await, re-check-after-await-before-any-state-write ordering in send(), and by confirming exactly four invalidation call sites exist (Clear conversation, Ask-memory reset, legacy restore, normal restore, Reset-everything — five semantically, four in Settings.tsx plus one in AskSheet.tsx). `askRequestGeneration`/`invalidateAskRequests` themselves are behaviorally tested (monotonic bump, captured-value mismatch after invalidation) in tests/coach-ask-memory.test.ts.
Focused failure reproduction and result: n/a — additive guard around an existing gap, no prior failing case to reproduce.
Full commands → exit/result/test counts/skips: `npx tsc --noEmit` exit 0; `npm run typecheck` exit 0; `npx tsc --noEmit -p proxy/tsconfig.json` exit 0; `npm test` — 58 files, 681 passed (675 + 6 new); `npm --prefix proxy test` — 87 passed, 8 skipped (unchanged); `npm run build` succeeded; `MARC_CHROMIUM=/opt/pw-browsers/chromium npm run gate` — PASS ×3 consecutive runs (confirms the happy path is unaffected).
Browser evidence paths, themes/widths and console/network result: the 3 gate runs above exercise the existing Coach/Train Ask flows unaffected.
Reviewer identity, findings, fixes and re-review: self-review; narrow, additive, source-and-unit-verified change with a clear mechanical invariant (generation captured, then re-checked, then and only then state is touched) rather than timing-dependent logic.
Commit and verified remote SHA: pending — recorded in the next entry once pushed and CI is confirmed.
Exact-head workflow URL + source/Android conclusions: pending, same reason.
Known limits / remaining proof: same honest gap as P03.1 — the actual live-browser race (open Ask, send, invalidate mid-flight, confirm the reply never appears) is not yet exercised end-to-end; source/unit coverage proves the mechanism is wired correctly, not that a real async race resolves as designed. Deferred to the same future browser-scenario work noted in P03.1's entry. The bulk of P03 — unifying Coach.tsx's and Train.tsx's separate AskSheet mounts into one App-level controller with shared context-by-IDs and draft state, contextual prefill from a presence launcher, no-nested-modal enforcement — has still not started.
Next concrete action: push this commit, verify exact-head CI, then start the host-unification design itself: a new controller module (open/closed state, surface + selected entity IDs, transient draft — NOT request generation, which now correctly lives in askMemory.ts already) that Coach.tsx and Train.tsx both read instead of their own separate `askOpen`/`buildingSplit` booleans, with App.tsx (or a shared parent) owning the single `<AskSheet>` mount.
```

```text
Patch / date / executor: P03.3 — shared host mount (the actual "unify" of P03) / 2026-09-21 / Claude Sonnet 5
Starting local and remote SHA: bae0273 (P03.2 request-generation invalidation, VERIFIED CI-green above), both local and origin.
User-visible change: opening "Ask Escobar" (from Coach's button, Train's Escobar button, or Coach's saved-review "Review"/"Review all") now opens the same shared sheet instance regardless of entry point. Switching tabs while it's open keeps it open — a typed-but-unsent question, an in-flight send or an error no longer vanish just from navigating away and back. It correctly stays closed (deferred, not stacked) if Settings is opened while it's already open, and reappears exactly as it was once Settings closes.
Owned files and any reconciled baseline drift: NEW src/slices/coach/askController.ts (askOpenState signal + openAsk/openAskSavedReview/closeAsk); src/slices/coach/Coach.tsx (removed its own askOpen/savedReview local state and <AskSheet> mounts, calls the controller instead); src/slices/workout/Train.tsx (removed buildingSplit local state and its <AskSheet> mount, calls openAsk); src/app/App.tsx (the one remaining <AskSheet> mount, gated on `askOpenState.value.open && !settingsOpen.value`); scripts/screenshot-gate.mjs (one existing scenario fixed — see Focused failure reproduction below); tests/ask-controller.test.ts (NEW, 9 tests), tests/coach-presence-ui.test.ts (1 assertion updated for the new onClick wiring). No other drift.
Architecture decisions: `askOpenState` lives outside CoachState (in-memory UI state, not persisted — matches `askRequestGeneration`'s own reasoning from P03.2) as a single object `{open, savedOnly, initialTurnKey?}` rather than three separate signals, so a caller can't accidentally set `savedOnly: true` while leaving a stale `initialTurnKey` from a previous open. Modal-stacking-with-Settings handling was the one piece added beyond the immediate "unify the mount" ask: moving AskSheet from per-tab-local to App-level made a genuinely new path reachable (open Ask, switch tabs, open Settings) that the old per-tab mount could never reach, since navigating away used to unmount AskSheet entirely. §4's own requirement ("show inline local help or defer the global modal... retain the draft") is met by the simplest correct form: suppress the render, touch nothing in askOpenState, so the sheet reappears untouched once Settings closes. Full defer-with-History/confirmation-sheets coverage (the rest of §4's "no nested modal" bullet) is explicitly NOT done this patch — Settings was the one new collision this specific refactor introduced, so it's the one fixed here; the others are pre-existing scope, named as remaining work below.
Regression IDs → concrete test/scenario names: A01 (one shared thread/draft/send-state from every entry point — by construction, since there is now only one AskSheet instance in the tree); A03's core claim (navigation cannot discard the draft) is now true by construction rather than needing separate re-implementation, though not yet exercised by a dedicated new browser scenario proving the draft literally survives a tab switch (see Known limits). New tests/ask-controller.test.ts: askOpenState transitions (start closed, openAsk, openAskSavedReview with/without a key, closeAsk always returns the same closed shape); Coach.tsx/Train.tsx no longer import or render <AskSheet> directly; App.tsx mounts exactly one, gated correctly, forwarding the right props.
Focused failure reproduction and result: **the full gate caught a real regression from this exact change, on the first run** — not shipped blind. scripts/screenshot-gate.mjs's existing "unfinished coach items" scenario applies a saved split draft (which internally calls `go('train')`), then clicks the Coach nav button again before reloading. In the OLD per-tab mount, navigating to Train unmounted Coach.tsx and its child AskSheet as a side effect, incidentally closing the still-open saved-review dialog — the test's later nav click implicitly relied on that. With AskSheet now correctly surviving navigation (the entire point of this patch), the dialog stayed open and blocked the next nav click — confirmed as the *intended* new behavior, not a bug, by re-reading Sheet's own close mechanics and this component's actual code paths, not assumed. Fixed by adding one explicit "Close" click to the gate script at that point, with a comment recording why — the old implicit close-via-unmount was itself the bug this whole P03 effort exists to remove. Full sweep of the rest of the gate script for the same class of scenario (open Ask/saved-review, then a nav click before an explicit close) found no other instance; the one other Ask-opening scenario (`undoPage`, a stale-Undo test) uses an isolated single-use browser context with no further navigation after opening it.
Full commands → exit/result/test counts/skips: `npx tsc --noEmit` exit 0; `npm run typecheck` exit 0; `npx tsc --noEmit -p proxy/tsconfig.json` exit 0; `npm test` — 59 files, 690 passed (681 + 9 new); `npm --prefix proxy test` — 87 passed, 8 skipped (unchanged); `npm run build` succeeded; `MARC_CHROMIUM=/opt/pw-browsers/chromium npm run gate` — **6 consecutive clean runs total** across this patch (3 that caught and confirmed the regression deterministically every time before the fix, then 1 immediately after fixing it, then 2 more after adding the new tests, for a total of 6 post-fix confirmations across the whole session for this patch alone).
Browser evidence paths, themes/widths and console/network result: the fixed "unfinished coach items" scenario itself is the browser evidence — it now exercises exactly the cross-navigation persistence this patch adds (apply a saved draft → navigate to Train under the hood → the review dialog is still open and must be explicitly closed → nav to Coach → reload → state persisted correctly).
Reviewer identity, findings, fixes and re-review: self-review, but the gate itself functioned as an adversarial check here and caught a real, non-obvious behavioral regression in an existing scenario — recorded in full above rather than just noting "fixed a test."
Commit and verified remote SHA: 7a3d874 (this patch's changeset); e8326b3 (a same-session, no-behavior-change follow-up that only added `.tmp/` to `.gitignore` — the gate script's local scratch backups had been left untracked). Both on `claude/phase-9-readiness-preference-ckw91g`.
Exact-head workflow URL + source/Android conclusions: https://github.com/macdarenz-droid/M-arc/actions/runs/35640250504 — source-gate success, android-gate success (APK built, verified, checksum uploaded), both for head_sha e8326b3a58048d359b06cf36818f1b34607e01aa, the exact commit this local worktree is now on. (The intermediate 7a3d874 push's own CI run, 35640202195, was superseded/cancelled by this immediately-following commit before it finished — expected GitHub behavior, not a failure; e8326b3's run is the one that actually ran end-to-end and is the authoritative result for this patch.)
Known limits / remaining proof: no NEW dedicated browser scenario was added proving a typed-but-unsent question literally survives a tab switch (A01/A03's most direct, user-visible claim) — the fixed unfinished-items scenario proves persistence across navigation for a *saved-review* dialog, which exercises the same underlying mechanism (the App-level mount) but isn't the identical "type a question, don't send, switch tabs, come back, see the same typed text" scenario. Worth adding as a follow-up, not urgent since the mechanism is shared. §4's "no nested modal" requirement remains open beyond the Settings case this patch specifically handled: AskSheet can still theoretically stack with an open History/Body detail sheet or a destructive confirmation dialog on Train, none of which call into askController and so aren't currently deferred against. Contextual prefill from a presence launcher's future "Ask about this" entry point (visible editable prefill, §4) has not been started — none of the four presence-integrated screens (Today/Train/History/Body) call openAsk from their PresenceLauncher yet; their "Open" action still routes to InsightSheet/SuggestionSheet, not Ask.
Next concrete action: either (a) extend the no-nested-modal deferral to History/Body detail sheets and Train's destructive confirmations, or (b) wire contextual prefill so a presence launcher can hand Ask a visible, editable starting question about the specific finding it was opened from — read A06 and the regression matrix's exact wording again before choosing which, since both are legitimate next slices of the same P03 scope and the choice affects what "P03 done" ultimately requires proving.
```

NEXT: P03.3 is CI-VERIFIED (exact-head run 35640250504, both jobs success). The shared host mount — the actual "unify Coach/Train's competing Ask mounts" piece, and the largest single slice of P03 — is shipped and confirmed on remote head e8326b3. A real regression it introduced in an existing gate scenario was caught and properly fixed, not worked around. P03.4 (below) verified, rather than assumed, that the broader no-nested-modal requirement is already met by construction for History/Body/Train — no new guard code was needed there, only a browser test proving it. Remaining P03 scope: contextual prefill from a presence launcher, and a dedicated typed-draft-survives-navigation browser scenario. Continue directly into one of those two — do not stop for a new prompt, per this package's own resume protocol.
```

```text
Patch / date / executor: P03.4 — verified no-nested-modal holds for History/Body/Train (no code fix needed) / 2026-09-21 / Claude Sonnet 5
Starting local and remote SHA: 907b33a (P03.3 CI-confirmation entry above), both local and origin.
User-visible change: none — this patch is a browser-verified architectural finding plus one new permanent gate scenario, not a behavior change.
Owned files and any reconciled baseline drift: scripts/screenshot-gate.mjs only (+1 new scenario, no production code touched). No drift.
Architecture decisions: before building the `localSheetCount`/Sheet-`blocking`-prop mechanism the previous entry's "Next concrete action" proposed, traced the actual reachability of every path that could put AskSheet's dialog and a second local sheet (History's session detail, Train's SplitEditor/ExercisePicker/swap-confirm, Body's info sheets) or Settings on screen at the same time: (1) `Sheet` (src/ui/primitives.tsx) opens with native `showModal()`, which the HTML spec makes every other on-page control inert while it's the topmost modal; (2) App.tsx only mounts the ACTIVE tab's screen component (`{t === 'today' && <Today />}` etc.) — a different tab's own trigger buttons (Today's Settings gear, History's rows, Train's edit/swap buttons) simply aren't in the DOM while viewing another tab; (3) the only way to change `tab.value` from *inside* an already-open AskSheet is its own two internal `go(...)` calls (SplitDraftAction → `go('train')`, ScheduleDraftAction → `go('coach')`) — neither ever targets `'today'`, `'history'` or `'body'`, so Today's Settings gear (the only Settings trigger in the app) can never even be rendered behind an open Ask dialog. Every other route to a second sheet requires clicking a trigger that lives outside the currently-open dialog's subtree, which inertness blocks. Concluded the "no nested modal" requirement (01-ARCHITECTURE.md §4 line 68; regression B03) is met by construction for History/Body/Train already, without new runtime guard code — building `localSheetCount` anyway would have added real surface area (a new prop threaded through the shared `Sheet` primitive used at 10+ call sites, two required opt-outs) to defend a structurally unreachable path, which is the wrong trade against this session's own "precision, not bugs" bar. Verified this reasoning empirically rather than shipping it as an assumption (see below) before writing it down.
Regression IDs → concrete test/scenario names: B03 ("no nested modal in History/Settings") — NEW gate scenario in scripts/screenshot-gate.mjs: opens Ask from Coach (with `remoteExplainer: true`, since the "Ask Escobar a question" button only renders when the online coach is on — an early run of this scenario timed out waiting for that exact button before this fixture flag was added, a real authoring catch, not a product bug), then attempts a real Playwright click on the bottom nav's "Today" button with a bounded 2s timeout; asserts the click times out (blocked by native modal inertness) AND the Ask dialog is still visible afterward (heading `Ask Escobar` still present) — i.e. proves nav is genuinely inert while Ask is open, which is the concrete mechanism that keeps every other tab's local sheets and Settings unreachable behind it.
Focused failure reproduction and result: first attempt failed for an unrelated reason — `TimeoutError` waiting for the "Ask Escobar a question" button, because the fixture state used didn't have `coach.remoteExplainer: true`, and that button only renders when `remoteEnabled.value` is true (src/slices/coach/Coach.tsx:96,104). Fixed by deriving a `nestedModalState` fixture with that flag set (matching the existing `undoPage` scenario's own pattern two blocks above it in the same file). After that fix, the actual claim under test — nav click blocked by inertness — passed on the very first run.
Full commands → exit/result/test counts/skips: `npx tsc --noEmit` exit 0 (no .ts/.tsx changed, ran anyway per the standard gate); `npm test` — 59 files, 690 passed (unchanged — a .mjs gate-script-only change has no unit-test surface); `npx tsc --noEmit -p proxy/tsconfig.json` exit 0; `npm --prefix proxy test` — 87 passed, 8 skipped (unchanged); `npm run build` succeeded; `MARC_CHROMIUM=/opt/pw-browsers/chromium npm run gate` — PASS ×3 consecutive runs (1 that fixed the fixture-flag authoring mistake, then 2 more confirming the real claim holds, not just once).
Browser evidence paths, themes/widths and console/network result: the new scenario itself is the evidence — a real bounded-timeout Playwright click against the live nav bar while a real native `<dialog>` is `showModal()`-open, in the same silent-black-themed, 390×844, isMobile+hasTouch context every other scenario in this file uses; no page errors, no console warnings from the blocked click (Playwright's own actionability-timeout, not a thrown app exception).
Reviewer identity, findings, fixes and re-review: self-review, but this patch is itself the outcome of independently re-examining the previous entry's own proposed next step before building it — a case worth recording plainly: the more expensive plan (new shared-primitive plumbing) was not built once the cheaper, more precise option (prove the platform already handles it) was verified to actually hold.
Commit and verified remote SHA: pending — recorded in the next entry once pushed and CI is confirmed.
Exact-head workflow URL + source/Android conclusions: pending, same reason.
Known limits / remaining proof: this proves the CURRENT navigation graph can't reach a nested-modal state — it is not a structural guarantee that survives every future change. Concretely, if a future patch adds any `go('today')` (or a new Settings/local-sheet trigger reachable from within AskSheet's own rendered content, or a global keyboard shortcut that bypasses inertness), this specific protection silently stops holding and nothing in the codebase would catch that regression except this one browser scenario re-running in CI on the changed commit — there is no source-level test asserting AskSheet's `go(...)` targets stay restricted to `'train'`/`'coach'` (worth adding as a cheap, permanent belt-and-suspenders unit check, not done this patch). If a future need genuinely requires opening Settings or a local sheet from *within* the Ask conversation itself (not something today's scope does), the `localSheetCount`/`blocking`-prop design sketched in the previous entry remains the right shape for that — just not needed yet.
Next concrete action: push this commit, verify exact-head CI, then take the remaining P03 scope: contextual prefill so a presence launcher can hand Ask a visible, editable starting question about the specific finding it was opened from (A06, the "Do" line's own "context by IDs, visible editable prefill"). Given the Train investigation's lesson that PresenceLauncher's own DOM structure is unusually fragile under the visual gate, add any new interactive element to it cautiously — isolate the change, run the full gate immediately, and don't stack it with other PresenceLauncher edits in the same attempt.
```

```text
Patch / date / executor: P03.5 — contextual prefill (A06) / 2026-09-21 / Claude Sonnet 5
Starting local and remote SHA: ee16dea (P03.4, CI-confirmed above), both local and origin.
User-visible change: InsightSheet and SuggestionSheet — the exact detail views every presence-integrated screen's (Today/Train/History/Body) "Open" tap already routes to, plus Coach's own Insights/Suggestions lists — now offer "Ask Escobar about this", which opens the shared Ask sheet with the composer pre-filled with `About "<title>": `, fully visible and editable, cursor ready to continue typing. Nothing is sent automatically.
Owned files and any reconciled baseline drift: src/slices/coach/askController.ts (+`initialQuestion` on `AskOpenState`, +`openAskWithQuestion`); src/slices/coach/AskSheet.tsx (+`initialQuestion` prop, seeds `question` state once at mount, clamped to `MAX_QUESTION_CHARS`); src/app/App.tsx (forwards `initialQuestion`); src/slices/coach/Coach.tsx (+"Ask Escobar about this" button in both InsightSheet and SuggestionSheet, closing the detail sheet first); tests/ask-controller.test.ts (+3), tests/ask-sheet-remote-guard.test.ts (+4); scripts/screenshot-gate.mjs (+1 new browser scenario). No drift.
Architecture decisions: deliberately did NOT touch `PresenceLauncher` (src/slices/coach/Presence.tsx) to add this entry point — given the Train investigation's own finding that this specific component's DOM structure is unusually fragile under the visual gate (documented in this ledger's own earlier entries), wiring "Ask about this" into the already-stable `InsightSheet`/`SuggestionSheet` instead delivers the same product value (every presence surface's "Open" tap already lands there) at materially lower risk, without touching the fragile component at all. `initialQuestion` is read once, at mount, via `useState(() => ...)` rather than a reactive effect — safe because AskSheet is a native modal dialog: a second "Ask about this" tap can't reach an already-open instance (this is exactly what P03.4's verified nested-modal reasoning, immediately above, establishes), so there is no live scenario needing to update an already-mounted instance's prefill. Both detail sheets call `onClose()` before `openAskWithQuestion(...)` so the outgoing sheet's own `<dialog>` cleanly closes before the shared Ask dialog opens, rather than two dialogs transiently existing at once.
Regression IDs → concrete test/scenario names: A06 ("Question prefill is visible/editable; 300-char limit never secretly truncates typed words for a prefix") — NEW gate scenario: opens an insight's detail sheet, taps "Ask Escobar about this", asserts the composer's real `<input>` value exactly matches the expected prefill, asserts opening it did NOT append a turn to `coach.askThread` (proving it's a seed, not a send), then clicks into the composer, moves the cursor to the end, types a continuation, and asserts the final value ends with the typed text (proving it's genuinely editable, not a frozen/read-only field). Source-check tests (tests/ask-sheet-remote-guard.test.ts, tests/ask-controller.test.ts) cover: `initialQuestion` is clamped to `MAX_QUESTION_CHARS`; nothing in the props-to-`send()` seeding path calls `send()`; both Coach.tsx call sites close the detail sheet before opening Ask; App.tsx forwards `initialQuestion` from the controller state.
Focused failure reproduction and result: the new gate scenario failed twice before passing, both authoring mistakes in the test itself, not the feature — recorded because they're easy to reintroduce: (1) first run asserted `coach.askThread.length === 0` after opening the prefilled sheet, which failed because the reused `unfinishedState`-derived fixture already carries earlier conversation turns from an unrelated scenario's own setup — fixed by comparing the thread length before vs. after the click instead of assuming zero; (2) `.type()`'s appended text did not land at the end of the existing value (Playwright's `.type()` respects the current caret, which a freshly-set controlled-input value does not guarantee is at the end) — fixed by explicitly clicking into the field and pressing `End` before typing. Once both were fixed, the actual claims under test (seed-not-send, genuinely editable) passed cleanly.
Full commands → exit/result/test counts/skips: `npx tsc --noEmit` exit 0; `npm run typecheck` exit 0; `npx tsc --noEmit -p proxy/tsconfig.json` exit 0; `npm test` — 59 files, 695 passed (690 + 5 new); `npm --prefix proxy test` — 87 passed, 8 skipped (unchanged); `npm run build` succeeded; `MARC_CHROMIUM=/opt/pw-browsers/chromium npm run gate` — PASS ×3 consecutive runs (after the 2 authoring-mistake fixes above, both caught by the gate itself failing honestly rather than silently passing).
Browser evidence paths, themes/widths and console/network result: the new scenario is the evidence — a real click through Coach → an insight's detail sheet → "Ask Escobar about this" → asserting the live composer's `input.value`, `localStorage` state, and post-edit value, all in the same silent-black/390×844/isMobile+hasTouch context every other scenario in this file uses. No page errors, no external requests attempted (opening a prefilled composer makes no network call by itself — `send()` is a separate, explicit action).
Reviewer identity, findings, fixes and re-review: self-review; both authoring mistakes above were caught by the gate script itself failing with a clear, specific message (not a generic crash), consistent with this session's established pattern of trusting the real browser check over an assumption.
Commit and verified remote SHA: pending — recorded in the next entry once pushed and CI is confirmed.
Exact-head workflow URL + source/Android conclusions: pending, same reason.
Known limits / remaining proof: the prefill is currently only reachable via InsightSheet/SuggestionSheet's detail view (reached through any presence-integrated screen's "Open" tap, or directly from Coach's own lists) — not from `PresenceLauncher` itself, a deliberate scope narrowing recorded above, not an oversight. A dedicated typed-draft-survives-navigation browser scenario (A01/A03's most direct claim) remains the one item from P03.3's own known limits not yet covered by any scenario — the new prefill scenario exercises a different path (seed-then-edit, not type-then-navigate-away-and-back) and doesn't substitute for it.
Next concrete action: push this commit, verify exact-head CI. With this, every item the "Do" line for P03 named is now shipped: unify Ask host (P03.3), context by IDs / visible editable prefill (P03.5), fresh lookup on send (already true — `send()` reads `report.value`/`state.value` live, not captured), shared saved review and draft (P03.3), no automatic submission (verified this patch), in-handler remote-enabled guard (P03.1), clear/reset/restore invalidates request generations (P03.2), no nested modal (verified in P03.4). The one still-open item is the dedicated typed-draft-survives-navigation scenario noted above — add it, then re-read P03's own Exit criteria fresh against everything shipped so far before marking P03 VERIFIED in the status table and starting P04.
```

```text
Patch / date / executor: P03.6 — typed-draft-survives-in-sheet-navigation scenario; P03 marked VERIFIED / 2026-09-21 / Claude Sonnet 5
Starting local and remote SHA: 1f72d9d (P03.5, CI-confirmed above), both local and origin.
User-visible change: none — this patch is one new permanent gate scenario proving an existing behavior, not a behavior change.
Owned files and any reconciled baseline drift: scripts/screenshot-gate.mjs only (+1 new scenario). No production code touched, no drift.
Architecture decisions: this scenario specifically targets what "a typed-but-unsent question survives navigation" actually means given P03.4's own verified finding that the bottom nav is genuinely inert while Ask is open — a real user cannot tap the nav bar mid-conversation to switch tabs, so "navigating away and back" while a draft sits typed can only happen through an action *inside* the already-open Ask sheet itself (e.g. applying a saved split draft, which calls `go('train')` internally without closing the dialog) — exactly the mechanism the P03.3 "unfinished coach items" fix made safe to exercise, but that fix's own scenario never asserted on the *typed draft's own text* surviving it, only that the saved-review dialog stayed open. This patch closes that specific gap.
Regression IDs → concrete test/scenario names: A01/A03 ("Navigation/close/reopen cannot double-send or silently discard draft") — NEW gate scenario: opens Ask (plain, non-savedOnly, from Coach), types a question into the real composer without sending, clicks an existing saved draft's "Create split: Saved full body" action (a genuine in-sheet action that calls `go('train')` internally), then asserts the composer's value is byte-identical to what was typed and the Ask dialog is still open — proving the internal navigation neither cleared nor double-submitted the draft.
Focused failure reproduction and result: the scenario's own closing `Close`-button click failed on its first run with Playwright's actionability check reporting the dialog itself as intercepting pointer events at that coordinate — most likely the smooth-scrolling thread (a new "Applied: ..." message appended after the draft is applied) shifting layout between Playwright's target-coordinate computation and the actual click. Not a product bug: switched the closing action from a coordinate-based button click to `Escape` (the same robust pattern every other scenario in this file already uses to close AskSheet), which resolved it immediately and is arguably the more correct choice regardless, since Escape is not sensitive to in-page layout shifts the way a positional click is.
Full commands → exit/result/test counts/skips: `npx tsc --noEmit` exit 0; `npm test` — 59 files, 695 passed (unchanged — gate-script-only change); `npx tsc --noEmit -p proxy/tsconfig.json` exit 0; `npm --prefix proxy test` — 87 passed, 8 skipped (unchanged); `npm run build` succeeded; `MARC_CHROMIUM=/opt/pw-browsers/chromium npm run gate` — PASS ×3 consecutive runs (1 that fixed the Close-click flakiness, then 2 more confirming the real claim holds).
Browser evidence paths, themes/widths and console/network result: the new scenario itself is the evidence — real typed input via `.fill()`, a real click on an existing draft's apply action, and a direct read of the composer's live `input.value` afterward, in the same silent-black/390×844/isMobile+hasTouch context every other scenario in this file uses. No page errors.
Reviewer identity, findings, fixes and re-review: self-review; the Close-click flakiness was caught and fixed by the gate itself failing with a specific, readable trace, not silently passing on a coincidence.
Commit and verified remote SHA: 6bf05ee2c1e0c74feb596894c60b5fe48f9dc9f7, `claude/phase-9-readiness-preference-ckw91g`.
Exact-head workflow URL + source/Android conclusions: https://github.com/macdarenz-droid/M-arc/actions/runs/35644471679 — source-gate success, android-gate success (APK built, verified), both for head_sha 6bf05ee2c1e0c74feb596894c60b5fe48f9dc9f7, the exact commit this local worktree is now on.
Known limits / remaining proof: this proves the in-sheet-action navigation path specifically (the only reachable "navigate while Ask stays open" path per P03.4's own finding) — it does not, and cannot, prove a plain nav-bar tab switch preserves a draft, because that interaction is not reachable while Ask is open in the first place (inertness blocks it, per P03.4). That is the correct, verified scope for this claim, not a gap. With this scenario shipped, every item P03's own "Do" line and Exit criteria named is now accounted for with either shipped code or a verified-not-needed finding — P03 is VERIFIED, CI-confirmed on this exact SHA.
Next concrete action: read `docs/escobar-presence/02-IMPLEMENTATION-WORKFLOW.md`'s P04 section (optional intent contract and safe normalization) and `01-ARCHITECTURE.md` §5 fresh before starting it — do not carry forward any P03-specific assumptions without re-checking them against P04's own scope.
```

```text
Patch / date / executor: P04 — optional intent contract and safe normalization / 2026-09-21 / Claude Sonnet 5
Starting local and remote SHA: 0278001 (P03 VERIFIED, CI-confirmed above), both local and origin.
User-visible change: a session captured while an already-accepted active deload is running now shows, throughout the live session, "Captured as an easier session: stop at {cap} effort, no max sets." — the session's own frozen intent, not the live deload state (which the pre-start Splits screen's existing banner already shows and which can change mid-session without retroactively altering what this session was captured as). A normal session shows nothing new, matching the app's existing restraint (no banner exists for "not in a deload" either).
Owned files and any reconciled baseline drift: src/core/models.ts (+`SessionIntent`, `PlanAgreementChange`, `SessionAssessment` types, `MAX_ASSESSMENT_CHANGES`, optional `WorkoutPlanSnapshot.assessment`); src/brain/coach/deload.ts (+`captureSessionIntent`); src/brain/debrief.ts (`capturePlan` now attaches `assessment`); src/core/store.ts (+`validAssessment`/`validAssessmentChange`/`acyclicReplacements`/`replayValid`/`validSeenWorkingRow`/`normalizePlan`, wired into both `normalizeSessionMetadata` and `normalizeActiveMetadata` in place of the bare `validPlan(...) ? ... : undefined` pattern); src/slices/workout/Train.tsx (LiveSession's new intent banner); NEW tests/session-intent.test.ts (28 tests); scripts/screenshot-gate.mjs (+1 new browser scenario). No drift to reconcile.
Architecture decisions: (1) `capturePlan` is the single existing call site (`startSession` in src/slices/workout/session.ts is its only caller) — rather than threading an explicit "is this a genuinely new session" flag through capturePlan's signature, this patch leans on that existing fact: capturing a plan snapshot IS starting a new session today, so `capturePlan` can safely always attach a fresh `assessment` (documented with a comment naming the invariant explicitly, so a future second call site can't silently violate P04's own "capture only for NEW sessions" rule without a human reading that comment first). (2) Malformed `assessment` metadata is dropped as a WHOLE unit (never partially salvaged, matching D06's "explicit unavailable, not a truncated effective plan") via a new `normalizePlan()` wrapper around the existing `validPlan()`, rather than baking assessment-validity into `validPlan` itself — this keeps `validPlan`'s existing all-or-nothing semantics (which correctly treat version/capturedAt/goal/deload/entries as structural, not optional) separate from `assessment`'s different, more lenient "optional metadata, drop-just-this-field" contract (D03), matching how `targetOverrides`/`coachDecision` are already individually stripped at the entry level in `normalizeActiveMetadata` rather than invalidating the whole entry. (3) The replay validator (`replayValid`) is real lifecycle-replay logic (active/retired entry tracking, double-introduction/double-retirement/targets-after-retirement/one-adjustment-per-entry rejection) per §5 ¶102's own requirement, not just static shape-checking — even though `changes` is always empty in practice today (P05 owns the mutation logic that would ever populate it), D04's own regression wording explicitly requires this validation to exist now, and building it against a real, empty-today journal means P05 has a working, already-tested validator the moment it starts appending real events, rather than needing to build one under deadline pressure later. (4) The active-session UI only surfaces the 'easier' case, not 'normal' — a deliberate scope narrowing, not an oversight, recorded with the same reasoning discipline as P02's Coach/Settings exemptions: a "Normal session" banner on every single workout forever would be exactly the kind of unearned, constant UI noise the whole Escobar-presence effort exists to avoid, and nothing in §5's wording requires a positive assertion for the unremarkable default case (the existing pattern for "not in a deload" is already silence, not a banner).
Regression IDs → concrete test/scenario names: I01 (tests/session-intent.test.ts: no active deload → normal, null cap, no RIR-band invention), I02 (active deload → easier, actual saved cap, both via `captureSessionIntent` directly and through `capturePlan`), I03/D07 (a plan with no `assessment` key at all — simulating pre-P04 data — stays without one after `loadState`, for both a saved session and a resumed active one; no backfill), I04 (goal/deload change AFTER `startSession` never rewrites the already-captured intent), I05 (pause/resume never touches `active.plan` at all — same object reference before/after), I06 (deload boundary inclusive on both ends, matching `deloadActive`'s own existing date policy — from/to both count as active). D01 (a valid assessment round-trips exactly through `loadState`, logged sets untouched). D03/D06 (unknown version, invalid `intent.kind`, invalid `effortCap`, non-ISO `capturedAt`, and an oversized 257-entry `changes` array each drop only `assessment`, keeping `plan.entries` and the session's logged sets intact — the oversized case specifically proving no silent truncation-to-256). D04, exhaustively: duplicate change `id`, a dangling `entryId` reference, a dangling `invalidatedEntryIds` reference, a cycle in `replace` edges (A→B→A), double introduction (two `add` events for the same entry), replacing into an already-active destination, double retirement (two `remove` events for the same entry), a `targets` event after that entry's own retirement, and a second `targets` event on the same entry (more than one live-adjustment) — each its own dedicated test proving the WHOLE assessment drops, plus one full valid case (a single `targets` event plus `seenWorkingRows`) proving the validator isn't simply rejecting everything. NEW browser scenario (scripts/screenshot-gate.mjs): a live session started under an active accepted deload shows the exact expected banner text on the real Train screen.
Focused failure reproduction and result: one authoring mistake caught immediately by the new test file itself — the "valid single targets event is accepted" case initially used the placeholder `capturedAt: 't'` (copied from the surrounding invalid-case tests, where the exact bad value never mattered), which is not a valid ISO timestamp and so correctly failed the validator's own date check; fixed by using a real ISO string for that one case, which then passed immediately. No other failures — every other test passed on first write, since each was authored directly against the validator's own already-implemented logic rather than a guessed contract.
Full commands → exit/result/test counts/skips: `npx tsc --noEmit` exit 0; `npm run typecheck` exit 0; `npx tsc --noEmit -p proxy/tsconfig.json` exit 0; `npm test` — 60 files, 723 passed (695 + 28 new); `npm --prefix proxy test` — 87 passed, 8 skipped (unchanged); `npm run build` succeeded; `MARC_CHROMIUM=/opt/pw-browsers/chromium npm run gate` — PASS ×3 consecutive runs (the new scenario passed on its first attempt, re-run twice more for confidence given the precision bar this session holds to).
Browser evidence paths, themes/widths and console/network result: the new scenario above is the browser evidence — a real active session seeded with an easier-intent assessment, navigated to via the real Train tab, asserting the exact banner text renders; the existing per-theme Train screenshot scenario also now renders the (empty, normal-case) banner-less path across all five themes with no page errors or overflow.
Reviewer identity, findings, fixes and re-review: self-review; this patch touches persistence/normalization (a "Persistence, journal, grounding and action changes require independent review before advancement" case per the workflow's own rule) — an independent reviewer is owed but not available this pass, flagged honestly rather than claimed. The validator logic was, however, exercised unusually thoroughly for a self-review: 19 of the 28 new tests are negative cases specifically targeting each named D04 failure mode individually, not just a couple of representative ones.
Commit and verified remote SHA: 6c066edcd07bf056351c987952e215aa207040b1, `claude/phase-9-readiness-preference-ckw91g`.
Exact-head workflow URL + source/Android conclusions: https://github.com/macdarenz-droid/M-arc/actions/runs/35646654197 — source-gate success, android-gate success (APK built, verified), both for head_sha 6c066edcd07bf056351c987952e215aa207040b1, the exact commit this local worktree is now on. P04 is VERIFIED.
Known limits / remaining proof: (a) D02's "browser" half (backup export/import round-trip specifically exercising an active session's `assessment` field) relies on the existing backup/restore gate scenarios' general active-workout coverage rather than a bespoke new scenario — the unit-level round-trip (`loadState` given a valid assessment) is directly tested and is the more precise proof of the validator's own correctness; the browser half is inherited coverage, not silently skipped, but also not newly, specifically proven. (b) P04's own "Do" line item "no mid-session intent/cap editing in this delivery" has nothing to violate it yet, since no UI offers such editing — correctly not built, not merely untested. (c) `changes`/`invalidatedEntryIds`/`seenWorkingRows` are always empty arrays in every real capture today, since P05 owns the mutation logic that would ever populate them — this patch's own replay/structural validators are exercised entirely through crafted test fixtures, not yet through any real user action, which is the correct and expected state for P04's own scope (P05 is exactly where these fixtures' shapes start appearing for real).
Next concrete action: push this commit, verify exact-head CI (both jobs), then read `01-ARCHITECTURE.md` §5's mutation rules (¶108-119) and P05's own "Do"/"Proof" lines in `02-IMPLEMENTATION-WORKFLOW.md` fresh before starting — P05 is real mutation-application logic (accepting live-adjustment offers, add/replace/remove, the seven numbered mutation rules) built directly on top of this patch's validator and types, not a continuation of P04's own read/validate-only scope, and its own workflow entry explicitly calls independent review of all mutation paths mandatory.
```

```text
Patch / date / executor: P05.1 — journal-append wiring / HANDOFF, review complete / CI unverified / 2026-09-21 / Claude Sonnet 5
Starting local and remote SHA: 22429d2 (P04 VERIFIED, CI-confirmed above), both local and origin.
STATUS: this session stopped here on the owner's stated credit limit. The mandatory independent review (below) came back clean — no blocking bugs, two minor test-description nits, both fixed in a follow-up commit — so this patch's CODE is sound. What's NOT done: exact-head CI has not been checked for either the original commit or the nit-fix follow-up commit. The next agent's first action must be verifying CI, not writing new code.
User-visible change: none new beyond the mechanism itself — accepting a live-adjustment offer, adding an exercise mid-session, swapping an exercise, or removing one now each append a matching PlanAgreementChange to the active session's plan.assessment.changes (P04's journal, previously always empty in practice). No UI surface changed.
Owned files and any reconciled baseline drift: src/brain/debrief.ts (+`appendAgreementChange`, the single append point enforcing the 256-change cap per D06); src/slices/workout/session.ts (`acceptLiveAdjustment`, `addExerciseToSession`, `replaceEntry`, `removeEntry` each now build and append the matching change alongside their existing, UNCHANGED plan.entries/targetOverrides/acceptedTargets mutations); NEW tests/plan-agreements.test.ts (9 tests). No drift to reconcile — reused the exact existing fixture/helper patterns from tests/live-adjustment-apply.test.ts and tests/session-plan.test.ts.
Architecture decisions: `appendAgreementChange` is a no-op (returns the plan unchanged) when `plan?.assessment` is absent — this is deliberate, not a gap: it's how a pre-P04 session (or any session whose assessment was already dropped by normalization) stays correctly un-backfilled per D07, even mid-session. One open design question this session did NOT resolve: whether every OTHER existing writer of `plan.entries`/`acceptedTargets` in the codebase besides these four functions has been found and accounted for — the four wired here were identified by grep for `capturePlanEntry(` and direct `plan.entries` mutation inside session.ts, but this was NOT independently cross-checked against the whole codebase before this session ran out of budget. This is exactly item 2's fourth bullet in the review brief below — unresolved, not dismissed.
Regression IDs → concrete test/scenario names: J03 (accepting a live adjustment appends the exact entry/reason/remaining-indices as a 'targets' change, tests/plan-agreements.test.ts), J06 (add/replace/remove each append their own matching change and correctly retire the original entry with excluded:'replaced'/'removed'), a pre-P04-session no-op guard for both the targets and add/replace/remove paths, a full mixed-session (add → targets → replace) round-trip through real `loadState` proving the P04 validator accepts real production output (not just hand-crafted fixtures — a meaningful cross-check the P04 patch itself couldn't do), and D06's overflow cap tested directly against `appendAgreementChange` (257th change drops the whole assessment, not a 256-length array).
Focused failure reproduction and result: one pure TypeScript authoring issue, not a logic bug — `removeEntry`'s existing (pre-P05) `excluded: 'removed'` object-literal property widened from the literal type `'removed'` to `string` once it moved from an inline ternary expression (which had direct contextual typing against `WorkoutPlanEntry['excluded']`) into a separately-named `const withEntries = ...` (which lost that context). Fixed with an explicit `as const`, matching the exact pattern `replaceEntry`'s pre-existing code already used for its own `'replaced' as const`. No behavioral bug found or introduced.
Full commands → exit/result/test counts/skips: `npx tsc --noEmit` exit 0; `npm test` — 61 files, 732 passed (723 + 9 new); `npx tsc --noEmit -p proxy/tsconfig.json` exit 0; `npm --prefix proxy test` — 87 passed, 8 skipped (unchanged); `npm run build` succeeded; `MARC_CHROMIUM=/opt/pw-browsers/chromium npm run gate` — PASS ×3 consecutive runs.
Browser evidence paths, themes/widths and console/network result: no NEW browser scenario was added this pass — the existing five-theme gate (which already exercises Train's live-session screen, add/swap/remove flows and effort-repair in several scenarios) passed clean with this wiring active, proving no regression, but does not specifically assert on `plan.assessment.changes` contents in the browser (that's unit-tested instead, per the established pattern from P04's own known-limits reasoning).
Reviewer identity, findings, fixes and re-review: **INDEPENDENT REVIEW COMPLETE.** Per P05's own workflow entry ("Independent review of all mutation paths is mandatory"), a separate Agent-tool subagent (not this session's own self-review) reviewed this exact diff against models.ts/store.ts's validator and every other codebase writer of `plan.entries`/`acceptedTargets`/`targetOverrides` (grepped the whole src/ tree — confirmed only 4 writers exist total: the 3 wired here plus `skipEntry`, correctly left unwired since P04's `PlanAgreementChange` union has no 'skip' variant). It independently re-ran `npx tsc --noEmit` and `npm test` itself rather than trusting this session's own results. Findings: **no blocking bugs, no real risks** — it specifically tried and confirmed-handled: double-'targets' on one entry (blocked upstream by `entry.coachDecision`/`decisionTaken` before any append is reached), double-'add' with the same exerciseId (blocked by `addExerciseToSession`'s own existing synchronous guard), dangling-link risk (ruled out — the plan.entries mutation and the journal append are built from the same literal values in one synchronous `patchActive` closure, no race window), `replaceEntry`'s `previous.planEntryId` undefined-guard (confirmed reachable only via an already-desynced entry that `normalizeActiveMetadata` itself produces, and confirmed it fails safely — entries and journal stay silently consistent together, not half-recorded), and D06 overflow's blast radius (confirmed the only other reader of `plan.assessment` in the whole codebase is Train.tsx's read-only "Captured as an easier session" banner — overflow degrades a UI reminder, never plan/logged-work integrity, since live autoregulation enforcement reads `state.coach.deload`/`plan.deload` directly, not `plan.assessment`). Two minor nits, both fixed in this same entry's commit: (1) tests/plan-agreements.test.ts's round-trip describe/it text claimed to cover "remove" but the test body never called `removeEntry` — renamed to accurately say "add, targets, replace" (removeEntry's own append is separately covered by the J06 describe block, just not through the save/reload round-trip); (2) the D06 overflow test built its 256-deep array as 256 'targets' events on one entryId, a shape the real replay validator would reject and so not representative of reachable production data — rebuilt as alternating add/remove across 128 distinct entryIds, a plausible churn shape, without changing what the test actually proves (the cap mechanism itself, tested directly against `appendAgreementChange`, independent of replay validity).
Commit and verified remote SHA: f82e19a (original wiring), plus a follow-up commit in this same session fixing the two nits above — recorded once that follow-up is pushed. **Neither commit's exact-head CI has been checked by this session.**
Exact-head workflow URL + source/Android conclusions: not yet checked — this is the very next action for whoever resumes, before any further code changes.
Known limits / remaining proof: (a) P05's remaining scope beyond this journal-append wiring is untouched: rule 4's seenWorkingRows/invalidatedEntryIds tracking (the "don't punish typing, do punish losing real work" distinction), rule 7's finish-time actualSetIndices interaction, and D05's "unexplained mismatch makes the entry unassessable" fallback are all still to be built — this patch (P05.1) is a deliberately narrow first slice of P05, not the whole of it; (b) no NEW browser scenario was added specifically asserting on `plan.assessment.changes` contents (the existing five-theme gate passed clean, proving no regression, but the journal itself is unit-tested, not browser-tested, matching the established pattern from P04).
Next concrete action, IN ORDER: (1) verify exact-head CI (both source-gate and android-gate) for the follow-up commit's exact SHA — do not proceed while it's red or unchecked; this is the only remaining step before P05.1 can be marked VERIFIED in the status table. (2) Once CI is confirmed, write the confirming PROGRESS.md/COACH_BRAIN.md update (mirroring the pattern used for every prior patch's own CI-confirmation entry in this file), then continue P05's remaining scope (rules 4 and 7, D05) per §5 ¶108-119 and the J01/J02/J04/J05/J07-J12/O01-O10 regression IDs not yet covered — read those sections fresh rather than assuming this entry's summary is complete.
```

```text
Patch / date / executor: P05.1 exact-head verification / 2026-09-22 / Codex
Starting local and remote SHA: 5cea4e238f1e46530a378bec7649ec84fd775041, both local and origin after a safe fast-forward; unrelated untracked `.tmp/` content preserved.
User-visible change: none; verification and ledger reconciliation only.
Owned files and any reconciled baseline drift: docs/escobar-presence/PROGRESS.md and docs/COACH_BRAIN.md only. The archive copy of this ledger was stale; the remote branch contained the authoritative P00-P05.1 sequence.
Architecture decisions (or none): none.
Regression IDs → concrete test/scenario names: inherited P05.1 evidence for J03/J06/D06 from the preceding record; no new behavior claimed.
Focused failure reproduction and result: not applicable; exact-head CI was the missing proof.
Full commands → exit/result/test counts/skips: GitHub Actions run 35648617281 completed successfully for exact head 5cea4e238f1e46530a378bec7649ec84fd775041. `source-gate` job 106495654295 succeeded; `android-gate` job 106496603257 succeeded.
Browser evidence paths, themes/widths and console/network result: inherited from P05.1's locally passing full gate; exact-head source-gate confirmed it remotely.
Reviewer identity, findings, fixes and re-review: inherited independent mutation-path review from the preceding P05.1 record; its two description/fixture nits are included in the verified SHA.
Commit and verified remote SHA: 5cea4e238f1e46530a378bec7649ec84fd775041.
Exact-head workflow URL + source/Android conclusions: https://github.com/macdarenz-droid/M-arc/actions/runs/35648617281 — source-gate success and android-gate success for the exact SHA.
Known limits / remaining proof: P05 remains IN PROGRESS. J01/J02/J04/J05/J07-J12, D05 and the named O regressions still require reconciliation or new proof; seenWorkingRows, invalidatedEntryIds, finish-time actualSetIndices and History structural invalidation are not implemented by P05.1.
Next concrete action: implement P05's remaining evidence-identity and invalidation rules, beginning with an ownership/writer audit and focused failing tests before production edits.
```

```text
Patch / date / executor: P05.2 — sticky row provenance, structural invalidation and projection reconciliation / 2026-09-22 / Codex
Starting local and remote SHA: local 0ded4257b433e68eae6adcf4c972bd054fe90a21 (P05.1 CI record); remote 5cea4e238f1e46530a378bec7649ec84fd775041. HTTPS Git credentials were unavailable in this task, so remote publication is pending; the connected GitHub API remains an available publication path.
User-visible change: accepted live targets now survive adding a set; ordinary same-row corrections stay assessable; deleting/reordering a row, clearing real work before removing/swapping it, or deleting saved History evidence is retained as uncertainty rather than being misreported as success.
Owned files and any reconciled baseline drift: src/brain/debrief.ts (sticky provenance/invalidation helpers); src/slices/workout/session.ts (set/add/remove/swap/finish boundaries); src/slices/history/sessionEdit.ts (saved structural deletion); src/core/store.ts (duplicate provenance rejection and journal/projection reconciliation); tests/plan-evidence-integrity.test.ts plus focused existing tests. Also renamed case-colliding src/slices/coach/presence.ts to presenceState.ts and updated imports because Windows resolved it as the existing Presence.tsx, preventing local typecheck; normalized one source-test's CRLF handling for the same portability reason.
Architecture decisions: (1) seenWorkingRows records row identity only when existing isWorkingSet semantics first become true; clearing/retyping never removes it. (2) Structural removeSet always invalidates the entry; destructive entry remove/swap invalidates only when real work exists now or was seen earlier, so approved untouched retirement remains assessable. (3) Finish adds invalidation when a previously working row is now absent/empty while retaining valid actualSetIndices for surviving rows. (4) History value/effort edits preserve mapping; filtering any blank row marks the linked entry invalid, including deletion of its final logged row. (5) Normalization keeps logs and the plan but adds the surviving entry to invalidatedEntryIds when journal targets, acceptedTargets and live targetOverrides disagree; retired entries are exempt from live-projection requirements. (6) A replacement from an already-unlinked active entry drops only the optional assessment rather than creating an invalid journal graph.
Regression IDs → concrete test/scenario names: J01/J02 (remaining-row kg-only/effort-only/duration/distance partial drafts reject stale acceptance byte-identically, tests/live-adjustment-apply.test.ts); J04/J05 (append preserves targetOverrides and appends null, copied extra is seen but has no accepted target; explicit extra index accepted within metadata bounds); J08 (sticky working provenance through clear/retype/reload, row deletion, clear-then-remove/swap and finish lost-evidence cases); J09 (existing session-plan empty-middle-row actualSetIndices plus new finish survivor mapping); J10/J11 (History value/effort preservation, row/final-row invalidation, existing stale-save/deletion guards); J12/O01/O02 (fresh A→B→A2 IDs and coherent replace journal, existing new-work Undo guard and same-count fingerprint suite); D04 (duplicate seenWorkingRows entry rejected); D05 (projection mismatch becomes explicit per-entry invalidation without dropping logs). Existing P05.1 tests continue covering J03/J06/D06; skip/remove behavior remains journal-covered and feeds P06's semantic proof.
Focused failure reproduction and result: the pre-change live-adjustment regression explicitly expected addSet to erase targetOverrides; updated behavior and test now preserve prior indices and add a null extra slot. A full-suite source test then failed solely because a CRLF checkout did not match a hard-coded LF substring; the source is now normalized in that test and the full suite passes on Windows. No product regression remained.
Full commands → exit/result/test counts/skips: app TypeScript exit 0; focused affected suite 7 files / 111 tests passed; full app suite 62 files / 746 tests passed; proxy TypeScript exit 0; proxy suite 87 passed / 8 conditional skips; production Vite build succeeded (146 modules); direct screenshot gate with installed Chrome passed all five themes with no page errors and legacy import verified.
Browser evidence paths, themes/widths and console/network result: scripts/screenshot-gate.mjs ran at its 390×844 mobile context for silent-black, paper, ember, emerald and midnight; PASS, no console/page errors, legacy storage untouched after import. Screenshots remain in the repository's ignored screenshots output.
Reviewer identity, findings, fixes and re-review: fresh self-review only. The workflow's independent mutation-path review requirement remains outstanding; no independent reviewer was created because this task was not authorized to delegate or create another task.
Commit and verified remote SHA: ea2313094997b1fa77a54ab555ff6995073a190f.
Exact-head workflow URL + source/Android conclusions: https://github.com/macdarenz-droid/M-arc/actions/runs/35680122450 — source-gate success and android-gate success for exact head ea2313094997b1fa77a54ab555ff6995073a190f.
Known limits / remaining proof: independent review is still required before P05 can become VERIFIED. J07's plan-fit meaning (skip stays expected; untouched removal retires; removing everything is insufficient) and replacement-chain counting are implemented as journal facts here and receive outcome-level proof in P06's projector. No new browser assertion reads internal journal arrays; deterministic unit tests cover them while both the local and remote full UI gates prove no surface regression.
Next concrete action: obtain the outstanding independent mutation review when authorized/available; P06's pure projector was implemented next without claiming that this substitutes for P05's independent review.
```

```text
Patch / date / executor: P06 — deterministic fair plan-fit projection / 2026-09-22 / Codex
Starting local and remote SHA: ea2313094997b1fa77a54ab555ff6995073a190f, exact-head source/android green.
User-visible change: no rendered surface yet. The app now has a pure local classifier that can distinguish Followed the plan, Harder than planned, Less work than planned, Followed the adjusted plan and Not enough information using the saved agreement and actual row identities.
Owned files and any reconciled baseline drift: NEW src/brain/planFit.ts and NEW tests/plan-fit.test.ts only, plus this ledger/decision log. Existing debrief and PR engines were not changed.
Architecture decisions: expected slots are keyed by plan entry ID plus original row index and derived by replaying the journal in stored order. Only final active obligations count; add-below keeps both, replacement chains keep only the final destination, remove retires, and skip does not retire. Accepted target events overlay only named indices and may explicitly adopt a bounded extra row; original targets remain separate. Actuals pair only through valid actualSetIndices. Canonical kg is compared with DEBRIEF_LOAD_EPS_KG; bodyweight uses reps and duration uses seconds. Starter/unavailable targets, unsupported modes, different loads, invalid mappings, invalidated entries, missing required effort and unadopted extras remain uncertainty. A confidently linked saved-cap breach has precedence and reports incomplete coverage when other evidence is unknown. PR calculation remains entirely independent in the existing engine.
Regression IDs → concrete test/scenario names: tests/plan-fit.test.ts has 20 tests covering F01–F16 and I07, plus J06 add-below, J07 skip-versus-remove, retired invalidation, duplicate/fresh replacement chains and bounded malformed-journal safety. F03 explicitly calls the existing PR engine and proves a real PR remains present beside Harder than planned. F15 compares equivalent canonical kg reached through a lb round-trip. Existing debrief/PR/execution/near-miss suites were rerun unchanged.
Focused failure reproduction and result: no product-code failure after the focused table was written. One test authoring parenthesis error was corrected before execution. The focused P06/debrief/PR set then passed 62/62.
Full commands → exit/result/test counts/skips: app TypeScript exit 0; full app suite 63 files / 766 tests passed; proxy TypeScript exit 0; proxy suite 87 passed / 8 conditional skips; production Vite build succeeded (146 modules); screenshot gate passed all five themes.
Browser evidence paths, themes/widths and console/network result: existing 390×844 gate for silent-black, paper, ember, emerald and midnight passed with no page errors and legacy import verified. P06 is pure/non-rendered, so no new screenshot surface was invented.
Reviewer identity, findings, fixes and re-review: fresh self-review only; no independent reviewer was created without delegation authorization. Reviewed replay lifecycle, projection mismatch behavior, cap precedence, invalidated/retired evidence, extra rows and malformed runtime inputs.
Commit and verified remote SHA: 18c86f7048fdf31894bce74d487197149fa7d8f8.
Exact-head workflow URL + source/Android conclusions: https://github.com/macdarenz-droid/M-arc/actions/runs/35681530569 — source-gate success and android-gate success for exact head 18c86f7048fdf31894bce74d487197149fa7d8f8.
Known limits / remaining proof: P06 is not yet rendered; that is P07. Independent review is not claimed. P05's independent mutation review also remains outstanding.
Next concrete action: integrate the verified result into the purpose-aware debrief in P07 without merging it with achievement/PR output.
```

```text
Patch / date / executor: P07 — purpose-aware debrief and consistent coaching copy / 2026-09-22 / Codex
Starting local and remote SHA: 18c86f7048fdf31894bce74d487197149fa7d8f8, exact-head source/android green.
User-visible change: Finish and History now show three deliberately separate sections: Achievement, Plan fit and Evidence. A record stays visible beside an adverse plan-fit label. Evidence names original targets, accepted targets and actual work instead of collapsing them into one number. Today's completed-workout card and Coach's new Latest workout section use the same local result and wording; Coach links to History for full evidence.
Owned files and any reconciled baseline drift: NEW src/brain/coach/planFitWords.ts and sessionFeedback.ts; src/brain/prs.ts; src/app/selectors.ts; finish/History/Today/Coach UI; screenshot gate; NEW focused wording/selector/UI tests plus PR coverage. The already-uncommitted P06 exact-CI ledger correction is included in this checkpoint.
Architecture decisions: (1) Achievement is computed by the existing PR engine through a new exact-session adapter; plan fit remains the P06 result and never changes PR semantics. (2) Steady/Direct affects summary wording only; headline, detail facts, classification, counts and achievements remain equal. (3) Today's status card reuses its occupied completed-session slot; Coach gets one Latest workout slot rather than duplicating the report-derived PresenceLauncher. (4) Only today's latest completed workout is selected for those summary surfaces, while History remains the durable place to inspect any older session. (5) Normal sessions explicitly say no effort ceiling was inferred; uncertainty and incomplete coverage stay next to the verdict, and not-logged wording is limited to the app's record.
Regression IDs → concrete test/scenario names: plan-fit-words.test.ts covers tone equality, normal/no-cap wording, starter/different-load/deleted-evidence/unadopted uncertainty, cap breach plus incomplete coverage and careful not-logged copy; session-feedback.test.ts covers same-day latest selection and tone-invariant facts/records; session-debrief-ui.test.ts locks the three sections and shared finish/History/Today/Coach wiring; prs.test.ts proves exact-session record attribution. The browser debrief fixture covers a real PR plus saved-cap breach, accepted easier target, starter uncertainty, extra work, expanded comparison, reload, export/restore, kg/lb and Steady/Direct facts.
Focused failure reproduction and result: the first browser run served the previous production bundle and correctly timed out on the new heading; rebuilding exposed one remaining old `Plan and actual` assertion in the earlier finish scenario. Updating that assertion to the new Evidence section made the gate pass. No product-code failure remained.
Full commands → exit/result/test counts/skips: app TypeScript exit 0; focused suite 5 files / 33 tests passed; full app suite 66 files / 776 tests passed; proxy TypeScript exit 0; proxy suite 87 passed / 8 conditional skips; production Vite build succeeded; direct screenshot gate passed all five themes.
Browser evidence paths, themes/widths and console/network result: scripts/screenshot-gate.mjs asserts Today, Coach, finish and History surfaces; Achievement/Plan fit/Evidence headings; Harder than planned plus New best; accepted original/actual provenance; reload and backup restore; kg and lb; Steady and Direct factual equality; expanded comparison; 320/360/900px overflow checks. The full silent-black, paper, ember, emerald and midnight run passed with no page errors, zero external requests in the debrief contexts and legacy import verified.
Reviewer identity, findings, fixes and re-review: fresh self-review only; no independent reviewer was created without delegation authorization. P07 does not mutate persistence or agreement journals; the outstanding mandatory independent review remains specifically P05.2's mutation paths.
Commit and verified remote SHA: b77bad419404c5f64f480bfd2a1eade209678c3a.
Exact-head workflow URL + source/Android conclusions: https://github.com/macdarenz-droid/M-arc/actions/runs/35683476449 — source-gate success and android-gate success for exact head b77bad419404c5f64f480bfd2a1eade209678c3a.
Known limits / remaining proof: the browser fixture starts from a deterministic saved post-workout agreement rather than replaying every F01–F16 interaction through UI; the pure projector already covers the full table and the rendered adapter is generic. P10 still owns the final integrated B10 audit and APK artifact. P05 cannot be called VERIFIED until its independent mutation review is available.
Next concrete action: begin P08's optional objective model without weakening the P05 review caveat.
```

```text
Patch / date / executor: P08 — optional local personal objective agreement / 2026-09-22 / Codex
Starting local and remote SHA: b0c16ec12972f0756d496173bd5147eb850176d4; exact-head run 35683899332 passed source-gate and android-gate.
User-visible change: Coach now has an optional “Your direction” agreement. The user can explicitly save a bounded statement, up to three priority muscles, availability notes, an equipment note, one to three evidence measures and an optional future review day. Cancel writes nothing; remove offers Undo. The summary says plainly that availability/equipment do not alter schedule, splits or targets.
Owned files and any reconciled baseline drift: src/core/models.ts (versioned objective types/caps); src/core/store.ts (objective-only normalizer); NEW src/slices/coach/objective.ts (guarded save/remove/restore); NEW ObjectiveAgreement.tsx (summary/editor); Coach.tsx integration; textarea theme styling; NEW tests/coach-objective.test.ts; mandatory browser scenario. No unrelated baseline drift.
Architecture decisions: (1) the objective is optional under coach.objective and absent on old/fresh state; no migration/backfill. (2) Save owns stable ID/revision/timestamps and writes only coach.objective; goal/schedule/splits/sessions are neither derived nor changed. (3) normalization accepts a structurally valid unknown lift ID so a deleted custom exercise remains visible as unavailable instead of deleting the user's statement. (4) a newly selected review day must be future, while an unchanged overdue day may be saved or imported so review remains possible. (5) duplicate muscles/days/measures are normalized deterministically; malformed or oversized objective metadata drops only objective. (6) objective prose/equipment/evidence arrays stay local and are not added to Ask or explain payloads.
Regression IDs → concrete test/scenario names: tests/coach-objective.test.ts covers G01 explicit save/cancel/fresh absence; G02 save/update/remove/Undo/revision/reload JSON round-trip; G03 invalid enum/day/date/oversized text/duplicate measures and missing exercise; G04 no mutation to goal/split/schedule/sessions; G05 later goal choice survives objective update; D01-D03 optional-field loading and objective-only drop; A07 local secrets absent from current explain payload and AskSheet construction. Browser gate performs cancel, save, local persistence, reload, schedule/split byte-equivalence, 320/900px layout and zero external requests.
Focused failure reproduction and result: the first browser draft used the gate's `day(14)` helper as though it meant future, but that helper subtracts offsets; the UI correctly rejected the past date. After switching to `day(-14)`, reload still appeared to lose the objective because the scenario's addInitScript unconditionally rewrote localStorage on every reload. Making fixture seeding conditional exposed the real saved state and the scenario passed. One repeated full suite run exceeded the existing fuzz performance threshold under concurrent local load; the isolated fuzz suite passed, and a fresh complete run passed 790/790 without changing thresholds or adding skips.
Full commands → exit/result/test counts/skips: app TypeScript exit 0; focused objective/presence/privacy suite 102 tests passed; full app suite 67 files / 790 tests passed; proxy TypeScript exit 0; proxy suite 87 passed / 8 existing conditional skips; production Vite build succeeded (151 modules); five-theme screenshot gate passed.
Browser evidence paths, themes/widths and console/network result: silent-black-objective-summary.png records the saved agreement; save/cancel/reload and 320/900px checks run in the real browser. The complete silent-black, paper, ember, emerald and midnight gate passed with no page errors and legacy import verified. The objective context recorded zero external requests.
Reviewer identity, findings, fixes and re-review: fresh self-review covered bounds, unknown references, clock rollback, stale restore, state isolation and remote payload boundaries. A separate reviewer was not created because this task has no authorization to delegate; under the workflow's persistence rule, independent review remains outstanding and P08 is not marked VERIFIED.
Commit and verified remote SHA: 292330651758bcf94aefa5b0a4026b804b1bbd82.
Exact-head workflow URL + source/Android conclusions: https://github.com/macdarenz-droid/M-arc/actions/runs/35686321589 — source-gate success and android-gate success for exact head 292330651758bcf94aefa5b0a4026b804b1bbd82.
Known limits / remaining proof: P09 owns evidence projections and due-review cues. P05.2 and P08 both retain explicit independent-review debt. Full five-theme inspection of populated objective/review states remains part of P10's integrated audit.
Next concrete action: obtain the outstanding independent persistence review when authorization permits; P09 proceeded without claiming that debt was cleared.
```

```text
Patch / date / executor: P09 — objective evidence review and surface integration / 2026-09-22 / Codex
Starting local and remote SHA: 292330651758bcf94aefa5b0a4026b804b1bbd82; exact-head run 35686321589 passed source-gate and android-gate.
User-visible change: Coach can open a full evidence review for the current objective revision. Each selected measure names its dated window, source, observation count, confidence and limitation; insufficient data stays unknown. Today can use one lower-priority in-app due-review cue, Body explains selected local tape estimates and the exposure-not-growth boundary, and Train shows the saved purpose without changing a programme.
Owned files and any reconciled baseline drift: NEW src/brain/coach/objectiveReview.ts and tests/coach-objective-review.test.ts; selected-input memoization in src/app/selectors.ts; ObjectiveAgreement/Coach/Today/Body/Train presentation; screenshot gate; progress and coach-brain ledgers. No proxy contract, persistence shape, reminder or dependency changed.
Architecture decisions: (1) projection is pure and carries objective ID/revision plus a deterministic evidence key over the records actually used; revision, relevant edit or deletion invalidates an open review. (2) Attendance uses completed Monday–Sunday weeks and distinct working-set days; lift evidence uses mode-appropriate best-per-day values from the existing exercise-history/trend adapters; body evidence uses only locally stored US Navy tape estimates. (3) The existing trend engine's four comparable-point threshold is disclosed; fewer than two can never support a trend. (4) Rising/falling are descriptive, not success/failure, because objective prose is not parsed into a desired physiological direction. (5) No overall score or plan is inferred. The only review action opens the bounded objective editor; no unrestricted executor was added. (6) the due cue is derived in-app, schedules no notification and yields to existing higher-priority Today coaching content.
Regression IDs → concrete test/scenario names: tests/coach-objective-review.test.ts covers G06 dated sources/windows/unknowns, G07 revision/edit/delete evidence-key invalidation, G08 local estimate wording and unsupported exercise modes, G09 due/overdue with no reminder/network import, G10 editor-only unsupported action boundary, M01/M04/M06 stable pure identity, and PERF01/PERF02 selected-input reuse across tone/second ticks with one relevant refresh. Existing objective/privacy/goal-Undo suites cover A07/G05. The browser objective scenario now covers save/cancel/reload, sufficient and insufficient review rows, body limitation, Body/Train integration, 320/390/900 layouts and zero external requests.
Focused failure reproduction and result: the first gate run found an ambiguous Close locator because the review sheet correctly exposes both a labelled icon close and a text close; scoping the assertion to the visible dialog's text button fixed the test. No product defect remained.
Full commands → exit/result/test counts/skips: app TypeScript exit 0; focused objective suites 20 tests passed; full app suite 68 files / 796 tests passed; proxy TypeScript exit 0; proxy suite 87 passed / 8 existing conditional skips; production Vite build succeeded (152 modules); five-theme screenshot gate passed.
Browser evidence paths, themes/widths and console/network result: silent-black-objective-review.png plus the existing summary image; the complete silent-black, paper, ember, emerald and midnight gate passed with no page errors and legacy import verified. Objective flow checks 320/390/900px and recorded zero external requests.
Reviewer identity, findings, fixes and re-review: fresh self-review only. P09 adds no persistence or mutation action. The workflow's P05.2/P08 independent review debt remains explicitly outstanding and P10's final independent review has not occurred.
Commit and verified remote SHA: a2a1f48217a61fbf9245c509ea7ec0f7b2b6ff21.
Exact-head workflow URL + source/Android conclusions: https://github.com/macdarenz-droid/M-arc/actions/runs/35688951369 — source-gate success (job 106621651764) and android-gate success (job 106622068443) for exact head a2a1f48217a61fbf9245c509ea7ec0f7b2b6ff21.
Known limits / remaining proof: Today intentionally suppresses the due-review cue when a higher-priority existing coaching moment or proposal already occupies the slot. Opening a review does not silently mark it complete; the chosen day remains due until the user edits or removes the objective. P10 still owns the combined legacy-to-review audit, independent review and final APK evidence.
Next concrete action: complete P10's final combined audit and delivery without overstating the outstanding independent review.
```

```text
Patch / date / executor: P10 — integrated audit and delivery checkpoint / 2026-09-22 / Codex
Starting local and remote SHA: a2a1f48217a61fbf9245c509ea7ec0f7b2b6ff21; exact-head run 35688951369 passed source-gate and android-gate.
User-visible change: no new scope. Final integration polish makes Body use the same Rising/Falling/Steady labels as the full review. The browser matrix now captures populated objective evidence on Body, Coach and Train in every theme and proves objective backup/restore.
Owned files and any reconciled baseline drift: scripts/screenshot-gate.mjs, src/slices/body/Body.tsx, PROJECT_STATE.md and documentation ledgers only. The baseline-to-head audit found no proxy, workflow, package/dependency or Capacitor configuration changes; `.gitignore` only gained the gate's `.tmp/` artifact directory earlier in the workflow.
Architecture decisions (or none): no new product decision. Cross-theme objective fixtures run in isolated contexts so they cannot overwrite the main legacy-import/workout lifecycle. Review direction remains descriptive, not a score.
Regression IDs → concrete test/scenario names: the combined permanent screenshot gate covers B01–B12 across its existing scenarios; the added P10 path covers populated objective Body/Coach/Train at 360/390 widths in silent-black, paper, ember, emerald and midnight, objective export/restore, zero external objective requests and the existing 320/900 stress checks. The full 796-test app suite retains M/D/A/G/I/J/F/O coverage mapped in prior patch records.
Focused failure reproduction and result: a first cross-theme fixture attempted to replace localStorage while the running app still owned the old in-memory state; its visibilitychange flush correctly restored the live state on reload. Moving the fixture to a fresh seeded browser context fixed the test without weakening app persistence. Visual inspection then found Body's raw `down` chip inconsistent with the full review's descriptive `Falling`; the label was aligned and rechecked.
Full commands → exit/result/test counts/skips: app typecheck, 68 files / 796 tests and production build pass; proxy typecheck plus 87 tests pass with 8 existing conditional skips; expanded five-theme visual/migration gate passes. No threshold, validation or skip changed.
Browser evidence paths, themes/widths and console/network result: `${theme}-objective-body.png`, `${theme}-objective-review.png`, `${theme}-objective-review-360.png` and `${theme}-objective-train.png` for all five themes, plus the focused 320/900 objective images. Visual inspection found readable wrapping/contrast and visible controls. No page errors; objective contexts made zero external requests. The focused objective backup restored its statement and opened the review from the restored state.
Reviewer identity, findings, fixes and re-review: fresh baseline-to-head self-review only. No independent reviewer was available without delegation authorization, so the workflow's required independent P05.2/P08/final review remains outstanding and P10 is not marked VERIFIED.
Commit and verified remote SHA: pending.
Exact-head workflow URL + source/Android conclusions: pending final push and exact-head CI.
Known limits / remaining proof: independent mutation/persistence/final review remains required. Physical phone installation is not tested. Final CI artifact and checksum must be verified after the final commit is pushed.
Next concrete action: run the exact final local gate, commit/push, verify both final jobs and the MARC-DEBUG-APK artifact/checksum; report the independent-review blocker plainly.
```

```text
Patch / date / executor: P05.3 — replay origin/link validation hardening / 2026-09-22 / Codex
Starting local and remote SHA: 621c32a6b42547e6adf0d00687b5e020457ff79d, whose exact-head run 35690338125 passed source-gate and android-gate.
User-visible change: none. A malformed imported optional assessment can no longer keep a seemingly valid agreement when its journal disagrees with captured entry origins/replacement links; the original plan and logged work remain intact while only assessment becomes unavailable.
Owned files and any reconciled baseline drift: src/core/store.ts replay validation; tests/plan-evidence-integrity.test.ts malformed lifecycle fixtures; this ledger/decision log. No mutation writer, UI, proxy, dependency or state-version change.
Architecture decisions: replay now receives the captured entries rather than only start IDs. Add must introduce an `origin: added` entry without `replaces`; replace must introduce `origin: replacement` whose `replaces` equals the event source; start/added entries cannot carry a replacement link; and every non-start captured entry must be introduced by exactly one replayed event. Existing active/retired/target ordering checks remain unchanged. Failure still drops the optional assessment only.
Regression IDs → concrete test/scenario names: D04 in tests/plan-evidence-integrity.test.ts now covers an orphaned added entry, add-to-replacement origin mismatch, replacement with the wrong predecessor, and a start entry carrying an invalid replacement link. The existing real production mixed-journal round trip remains the positive control.
Focused failure reproduction and result: fresh source audit found the validator checked IDs/order/cycles but not the architecture's required origin/replaces agreement. All four malformed fixtures were accepted before the production edit and now lose only assessment while preserving plan/logs.
Full commands → exit/result/test counts/skips: focused 3 files / 48 tests passed; app typecheck passed; isolated fuzz passed; the default parallel full suite passed every functional assertion twice but its known five-second aggregate fuzz sentinel timed out at 5.076s and 5.691s under worker contention. No threshold or skip changed. A full single-worker run passed 68 files / 797 tests, including the same performance sentinel at 4.724s. Proxy typecheck and 87 tests passed with 8 existing conditional skips; production build succeeded (152 modules).
Browser evidence paths, themes/widths and console/network result: the unchanged complete screenshot/migration gate passed silent-black, paper, ember, emerald and midnight with no page errors and legacy import verified.
Reviewer identity, findings, fixes and re-review: fresh self-audit found and fixed the origin/link gap. This is not independent review, so P05 remains IN PROGRESS under its explicit workflow rule.
Commit and verified remote SHA: pending.
Exact-head workflow URL + source/Android conclusions: pending commit/push.
Known limits / remaining proof: independent review of P05.2/P05.3 mutation and persistence paths is still mandatory before P05 can be marked VERIFIED. The two parallel fuzz timeouts are local load sensitivity, not hidden by a changed threshold; isolated and single-worker full proofs are green.
Next concrete action: commit/push this bounded hardening patch, verify exact-head source/android jobs, then retain the independent-review blocker unless a genuinely separate reviewer becomes authorized and available.
```
